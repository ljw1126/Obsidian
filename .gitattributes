# .gitattributes 파일 내용

# 텍스트 파일(Markdown 포함)에 대해 Git이 개행 문자를 자동으로 변환하지 않도록 설정
# 이는 Obsidian이 저장한 LF 개행 문자를 그대로 유지하도록 강제합니다.
*.md    text eol=lf
*.txt   text eol=lf
*.csv   text eol=lf

# 다른 파일 (예: 이미지, PDF)는 바이너리로 처리
*.png   binary
*.jpg   binary
*.pdf   binary
