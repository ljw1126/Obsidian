p277
```shell
npm install @dnd-kit/core @dnd-kit/sortable

npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

> 프론트엔드 개발을 시작하려고 해. TRD.md를 참고해서 드래그 앤 드롭에 필요한 패키지를 설치해줘.
- src/client 패키지에 작업 
	- components/
		- board/ : Board, Column, BoardContainer 등 
		- ticket/ : TicketCard, TicketModal, ticketForm 등
		- ui/ : Button, Modal, Badge, ConfirmDialog 
	- hooks/
		- useTickets.ts : 티켓 CRUD + 상태 관리 
	- api/
		- ticketApi.ts : API 호출 함수 (fetch 래퍼)
- src/shared : 프론트 / 백엔드 공유 코드
	- types/index.ts  : Ticket, BoardData 등 
	- validations/ticket.ts : Zod 스키마 (폼 + API 검증 공유)

이 구조의 핵심은 계층 간 분리이다. 
- `src/client/`
	- 브라우저에서만 실행되는 코드
- `src/server/`
	- 서버에서만 실행되는 코드 (5장)
- `src/shared/`
	- 양쪽에서 공유하는 타입과 검증 스키마


p279
- reference/image 파일 준비 

```shell
이제 FE 화면을 개발할 거야.
@reference/image/트렐로.png와 @reference/image/tika-wrieframe.png를 살펴보고, 
이 화면 구성에 맞는 핵심 스타일을 @app/globals.css에 정의해줘
- 화면 관련 스펙은 @docs/COMPONENT_SPEC.md 참고 
- 요구사항 정의는 @docs/REQUIREMENTS.md 참고
```
- `@app/globals.css` 내용이 틀림
	- https://github.com/claude-code-expert/todo-app/blob/chapter6-frontend/app/globals.css

p281 6.2 컴포넌트 테스트 환경 구성 
```shell
@docs/COMPONENT_SPEC.md를 기반으로 FE 구현 계획을 만들어줘.
- 일단 컴포넌트부터 컨테이너까지 bottom-up 순서
- Phase 별 그룹핑과 의존성 그래프
- 각 컴포넌트별 TDD 체크 리스크
  
docs/FRONTED_TASKS.md로 저장해줘
```
- `docs/FRONTED_TASKS.md` 신규 파일 생성
	- 마찬가지로 내용이 다르다. 어떤 양식이 더 가독성이 있는지 분석이 필요하다 
	- https://github.com/claude-code-expert/todo-app/blob/chapter6-frontend/docs/FRONTEND_TASKS.md

```shell
docs/FRONTED_TASKS.md를 생성했습니다.

---
문서 구조 요약:

의존성 그래프 — 5개 Phase를 단방향 DAG로 표현했습니다. Atomic UI가 최하위, RSC가 최상위.

Phase 구성 (13개 항목)

┌───────────────────────┬──────────────────────────────────────────┬───────────────────────────────────┐
│         Phase         │                   내용                   │               근거                │
├───────────────────────┼──────────────────────────────────────────┼───────────────────────────────────┤
│ 1 — Atomic UI         │ Badge, Button, Modal, ConfirmDialog      │ 외부 의존 없는 순수 표현 컴포넌트 │
├───────────────────────┼──────────────────────────────────────────┼───────────────────────────────────┤
│ 2 — Ticket Primitives │ TicketDetailView, TicketForm, TicketCard │ Phase 1 + Zod 스키마 의존         │
├───────────────────────┼──────────────────────────────────────────┼───────────────────────────────────┤
│ 3 — Board Layout      │ Column, FilterBar, BoardHeader, Board    │ Phase 2 + DnD 라이브러리 개입     │
├───────────────────────┼──────────────────────────────────────────┼───────────────────────────────────┤
│ 4 — Hook & Modal      │ ticketApi, useTickets, TicketModal       │ API 연동 + 낙관적 업데이트        │
├───────────────────────┼──────────────────────────────────────────┼───────────────────────────────────┤
│ 5 — Integration       │ BoardContainer, page.tsx                 │ 전체 연결 + RSC                   │
└───────────────────────┴──────────────────────────────────────────┴───────────────────────────────────┘

TDD 체크리스트 — TC-COMP-, TC-INT- 전체를 컴포넌트별로 분해했으며, DnD(useSortable, DndContext)는 테스트에서 mock 처리해야 함을 명시했습니다.
```


**프리뷰 페이지** 생성👀
- mock 데이터로 컴포넌트를 렌더링하는 갤러리 페이지다
- 컴포넌트를 하나씩 만들면서 중간 결과를 확인하려면 DB 없이면 동작하는 별도 페이지가 필요하다

```shell
FE 컴포넌트를 개발하면서 시각적으로 확인할 프리뷰 페이지를 만들어줘.
- app/preview/page.tsx (클라이언트 컴포넌트)
- 목 데이터로 개별 컴포넌트를 렌더링 하는 갤러리 형식
- 지금은 빈 컨테이너만 만들고, Phase별로 컴포넌트를 추가해 나갈 거야.
- DB 연결없이 npm run dev로 localhost:3000/preview에서 확인 가능해야 해
- 각 phase 개발이 완료되면 run test > 화면 확인 후 다음 과정으로 넘어가는거야
```
- 😱 와 .. 진짜 깔끔하게 잘 만들었다.

```shell
npm run dev 후 localhost:3000/preview 에서 바로 확인하실 수 있습니다.

혹시 이전 응답이 안 보이신다면 /compact로 컨텍스트가 압축되면서 메시지가 가려졌을 수 있습니다. 현재 파일 구조는 다음과 같습니다:

- 좌측 사이드바: Phase 1~5 네비게이션 + 전체 진행률 표시
- 우측 메인: Phase별 섹션 + 컴포넌트 슬롯 카드 (파일 경로, TC 참조, 미리보기 영역)
- 슬롯 상태: status: 'pending' (회색 플레이스홀더) → status: 'done' + preview: <컴포넌트 /> 추가 시 실제 렌더링

Phase 구현이 완료되면 PHASES 배열에서 해당 slot에 두 줄만 추가하면 갱신됩니다:
status: 'done',
preview: <Badge variant="priority-high" label="HIGH" />,
```
### 6.3 UI 컴포넌트 설계와 구현
p282 6.3.1 기본 컴포넌트 TDD로 구현하기
```shell
Button 컴포넌트의 테스트를 작성해줘. 아직 구현하지 말고 테스트만 (Red)

검증할 것:
- variant 별 CSS 클래스 (primary, secondary, danger, ghost)
- size별 CSS 클래스 (sm, md, lg)
- 기본값 variant=primary, size=md
- onClient 핸들러 호출
- isLoading=true -> 비활성화 + "처리중..." 표시
- isLoading=true 일 때 클릭 무시 
- children 정상 렌더링
```
- `npm test -- Button`로 테스트가 실패하는지 확인 (Red)
- Button.test.tsx 생성됨 
```shell
Red 확인됐습니다. Button.tsx 파일이 없으므로 모듈을 찾지 못해 실패합니다.

---
작성된 테스트 구조입니다:

Button (5개 describe 블록, 17개 케이스)
│
├── variant (5개)
│   ├── primary → btn--primary 클래스
│   ├── secondary → btn--secondary 클래스
│   ├── danger → btn--danger 클래스
│   ├── ghost → btn--ghost 클래스
│   └── 모든 variant에 공통 btn 클래스
│
├── size (3개)
│   ├── sm → btn--sm 클래스
│   ├── lg → btn--lg 클래스
│   └── md → sm/lg 클래스 없음
│
├── 기본값 (1개)
│   └── size 미전달 → md(기본) 동작
│
├── children (2개)
│   ├── 문자열 children 렌더링
│   └── JSX children 렌더링
│
├── onClick (2개)
│   ├── 클릭 시 핸들러 호출
│   └── onClick 미전달 → 에러 없음
│
├── isLoading (4개)
│   ├── isLoading=true → disabled
│   ├── isLoading=true → "처리중..." 텍스트
│   ├── isLoading=true → 클릭해도 onClick 미호출
│   └── isLoading=false → 정상 children 표시
│
└── disabled (2개)
    ├── disabled=true → disabled 속성
    └── disabled=true → onClick 미호출

테스트가 요구하는 구현 계약이 명확합니다:
- variant별 btn--{variant} 클래스
- size='sm'|'lg'만 btn--{size} 클래스, md는 추가 클래스 없음
- isLoading=true → disabled + children 대신 "처리중..." 텍스트
- disabled prop → disabled HTML 속성

Green(구현) 단계로 넘어갈 준비가 됐습니다. 바로 구현할까요?
```
- p283 테스트 실행 명령어 오타인가? `npm test -- Button` 실행됨



```shell
@docs/FRONTEND_TASKS.md의 Phase 1 나머지 컴포넌트를 TDD로 구현해줘
- Badge (PriorityBadge, DueDateBadge)
- Modal (5개 테스트 : isOpen, ESC 닫기, 오버레이 클릭 닫기, 컨텐츠 클릭 무시, role=dialog)
- ConfirmDialog (5개 테스트 : 메시지 표시, 확인 -> onConfirm, 취소 -> onCancel)

각 컴포넌트마다 Red >  Green > Refactor 사이클을 적용해
```
- `npm test -- --testPathPattern="Button|Modal|ConfirmDialog"`
- `npx tsc --noEmit`

p284
```shell
Phase 1 컴포넌트를 프리뷰 페이지에 추가해줘.
app/preview/page.tsx에:
- Button : 네 가지 variant를 나란히 렌더링 
- Button : isLoading 상태도 보여줘
- Modal : 버튼 클릭으로 열기/닫기
- ConfirmDialog : 버튼 클릭으로 열기
```
- dev 환경 실행후 `/preview` 확인


**👀 컴포넌트 테스트에서 검증해야 할 것들은 다음과 같다**
- **렌더링 검증** : 사용자에게 보여야 할 내용이 실제로 화면에 나타나는가?
- **조건부 렌더링 검증** : 상태에 따라 화면이 올바르게 변하는가?
- **상호 작용 검증** : 사용자 동작에 올바르게 반응하늑나?


p285 **6.3.2 레이아웃 구현하기**
Phase 2에서는 칸반 보드의 핵심 구조를 만든다. 

다음 순으로 구현한다
- TicketCard (카드)
- ColumnHeader (컬럼 헤더)
- Column (컬럼)
- Board (4컬럼 레이아웃)

이중 TikectCard의 TDD 사이클을 직접 경험한다. 
- 칸반 보드에서 가장 시각적으로 풍부한 

(1) Red
```shell
TEST_CASES.md의 TC-COMP0991 (TicketCard) 테스트 케이스를 
React Testing Library 테스트 코드로 변환해줘.

테스트 케이스:
- C001-1: 기본 렌더링 (제목, 우선순위 뱃지, 종료 예정일)
- C001-2: 오버듀 표시 (isOverdue=true > data-overdue 속성)
- C001-3: 완료 상태 (status=Done > ticket-card-done 클래스)
- C001-4: dueDate=null > 날짜 영역 숨김
- C001-5: 클릭 > onClick 호출
- C001-6: 긴 제목 말줄임 클래스
- C001-7: 우선순위별 뱃지 data-priority 속성 

dnd-kit은 mock 처리해줘. 아직 구현은 하지마. (Red)

```


```shell
TicketCard.test.tsx의 7개 테스트를 통과하는
TicketCard 컴포넌트를 구현해줘 
- PriorityBadge, DueDateBadge도 함께 구현 (Budge.tsx)
- globals.css의 .ticket-card 관련 클래스 활용
- data-overdue 속성으로 오버듀 스타일 제어
- 접근성 : role="button", aria-label, tabIndex, 키보드 Enter/Space
```

```shell
44/44 통과. 리그레션 없음.
구현 요약

Badge.tsx — 2개 컴포넌트 추가
- PriorityBadge: badge badge--priority-{low|medium|high} 클래스 + data-testid="priority-badge" + data-priority 속성
- DueDateBadge: ticket-card-due 클래스 + data-testid="ticket-due-date", 오버듀 시 ticket-card-due--overdue

TicketCard.tsx — 완전 구현
- useSortable로 드래그 소스 설정 ({...attributes} → {...listeners} → 명시 props 순 spread)
- 클래스: ticket-card 기본 + ticket-card--overdue / ticket-card--done / ticket-card--dragging 조건부 추가
- data-overdue="true" 속성은 isOverdue=true일 때만 설정
- 제목: ticket-card-title truncate — font 스타일 + Tailwind 말줄임
- role="button", tabIndex={0}, aria-label="티켓: {title}", Enter/Space 키보드 핸들러

globals.css — 2곳 변경
- .ticket-card-title: -webkit-line-clamp 제거 (Tailwind truncate가 담당)
- .ticket-card--done 추가: opacity: 0.72 + 제목 취소선
```


```shell
@docs/FRONTEND_TASKS.md Phase 2 나머지를 TDD로 구현해줘.
- ColumnHeader: 컬럼명 + 티켓 수 표시  // 나는 없는 상태다.👀
- Column : SortableContext + useDroppable + 빈 컬럼 안내 (3 tests)
- Board : Backlog 사이드바 + 3컬럼 메인 레이아웃 
  
dnd-kit은 mock으로 처리해줘
```
- 내용이 틀려서 이렇게 구현하면 안된다..💩 
- 항목은 지우고 처음과 끝만 질문 
	- `@docs/FRONTEND_TASKS.md Phase 2 나머지를 TDD로 구현해줘. dnd-kit은 mock으로 처리해줘`
- 아.. 나는 Phase 3로 분리가 되어 있는 형태다.😱
	- Board, Column 내용이 Phase3에 있었다.
	- +`Phase 2 컴포넌트를 프리뷰 페이지에 추가해줘.`
		- https://github.com/claude-code-expert/todo-app/blob/chapter6-frontend/docs/FRONTEND_TASKS.md

```shell
빌드 성공. /preview 페이지가 34.4 kB로 증가했습니다 (Phase 1: ~5 kB → Phase 2 컴포넌트 추가). Warning은 기존 Modal 테스트 파일의 미사용 변수로 새 코드와 무관합니다.

---
추가된 내용

┌──────────────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│     컴포넌트     │                                                 미리보기 내용                                                  │
├──────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ TicketDetailView │ ① 모든 날짜 설정 / ② startedAt·completedAt=null → - 표시, 두 케이스 나란히                                     │
├──────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ TicketForm       │ create/edit 모드 토글 버튼, edit 시 initialData 반영, 제출 후 JSON 결과 표시                                   │
├──────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ TicketCard       │ DndContext+SortableContext로 감싸서 4종 카드 렌더링 (정상 / 오버듀 / DONE / dueDate 없음), 클릭 시 티켓명 표시 │
└──────────────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

사이드바 진행률: Phase 1 4/4, Phase 2 3/3 완료 표시, 전체 7/13으로 갱신됩니다.
```


내가 직접 질문함
```shell
# 분리해서 요청한다
@docs/FRONTEND_TASKS.md Phase 3 컴포넌트의 테스트를 작성해줘
아직 구현하지마. 실패하는 테스트 생성 후 커밋 정리해줘. (Red)

@docs/FRONTEND_TASKS.md Phase 3 테스트를 통과하는 구현을 만들어줘.
테스트 통과 후 커밋 정리해줘 (Green)


# 프리뷰 추가
Phase 3 컴포넌트를 프리뷰 페이지에 추가해줘.
목 데이터:
- BACKLOG: 2개 (하나는 HIGH 우선순위)
- TODO: 3개 (하나는 isOverdue=true)
- IN_PROGRESS: 1개
- DONE: 1개 

Board 컴포넌트를 랜더링해서 4컬럼 레이아웃이 보이게 해줘.
```

```shell
완료. 결과 요약:

Red 단계 완성 — 14개 신규 테스트 전부 실패, 기존 57개 통과 유지.

┌─────────────────────────────────┬────────┬──────────────────────────┐
│              파일               │ 테스트 │            TC            │
├─────────────────────────────────┼────────┼──────────────────────────┤
│ Board/__tests__/Column.test.tsx │ 4개    │ C002-1~3 + onTicketClick │
├─────────────────────────────────┼────────┼──────────────────────────┤
│ __tests__/FilterBar.test.tsx    │ 5개    │ C004-1~4 + active class  │
├─────────────────────────────────┼────────┼──────────────────────────┤
│ __tests__/BoardHeader.test.tsx  │ 3개    │ 버튼·클릭·검색창         │
├─────────────────────────────────┼────────┼──────────────────────────┤
│ Board/__tests__/Board.test.tsx  │ 2개    │ C003-1~2                 │
└─────────────────────────────────┴────────┴──────────────────────────┘

핵심 설계 결정:
- data-testid="column-count": 카운트 뱃지를 신뢰성 있게 타겟팅
- FilterBar 버튼 텍스트: TEST_CASES.md 기준 "이번주 업무" / "일정이 초과된 업무"
- dnd-kit 3개 패키지(@dnd-kit/core, sortable, utilities) 모두 mock — Column 내 TicketCard의 useSortable까지 커버
- Board 테스트: .board-sidebar / .board-columns CSS 클래스로 레이아웃 구조 검증

```

```shell
Phase 3 Green 완료. 결과 요약:

71/71 통과 (Phase 1~3 전체), TypeScript 에러 없음.

┌─────────────┬───────────────────────────────────────────────────────────────────────────────────┐
│  컴포넌트   │                                     핵심 구현                                     │
├─────────────┼───────────────────────────────────────────────────────────────────────────────────┤
│ Column      │ useDroppable + SortableContext, data-testid="column-count", 빈 칼럼 안내          │
├─────────────┼───────────────────────────────────────────────────────────────────────────────────┤
│ FilterBar   │ activeFilter === filter ? 'all' : filter 토글 패턴, filter-btn--active 클래스     │
├─────────────┼───────────────────────────────────────────────────────────────────────────────────┤
│ BoardHeader │ Button variant="primary", disabled 검색창                                         │
├─────────────┼───────────────────────────────────────────────────────────────────────────────────┤
│ Board       │ board-sidebar(BACKLOG) + board-columns(나머지 3개), DndContext + DragOverlay 래핑 │
└─────────────┴───────────────────────────────────────────────────────────────────────────────────┘
```

```shell
완료. localhost:3000/preview → Phase 3 섹션에서 확인할 수 있는 내용:

┌─────────────┬─────────────────────────────────────────────────────────────────────────────┐
│    슬롯     │                                미리보기 내용                                │
├─────────────┼─────────────────────────────────────────────────────────────────────────────┤
│ Column      │ TODO 3개(HIGH·isOverdue 포함) + 빈 IN_PROGRESS 칼럼, 카드 클릭 피드백       │
├─────────────┼─────────────────────────────────────────────────────────────────────────────┤
│ FilterBar   │ 다크 배경 위 필터 버튼, 클릭 시 토글·재클릭 시 해제, activeFilter 상태 표시 │
├─────────────┼─────────────────────────────────────────────────────────────────────────────┤
│ BoardHeader │ board-header 스타일 그대로, "새 티켓" 클릭 피드백, 검색창 비활성            │
├─────────────┼─────────────────────────────────────────────────────────────────────────────┤
│ Board       │ 4컬럼 전체 레이아웃 (480px 컨테이너 클립), 우측 컬럼 가로 스크롤 가능       │
└─────────────┴─────────────────────────────────────────────────────────────────────────────┘

사이드바 진행률: Phase 3 4/4 완료, 전체 11/13.
```

컴포넌트 에러 수정 
```shell
preview를 확인하는데 console error가 확인됩니다. src/client/components/Ticket/TicketCard.tsx (35:5) 에 div 태그가 문제인가 봅니다.  A tree hydrated but some attributes of the server rendered HTML didn't match the client properties. This won't be patched up. This can happen if a SSR-ed Client Component used:


원인 요약:

dnd-kit의 useSortable은 두 가지 SSR 불일치를 일으킵니다.

┌─────────────────────────────────────┬─────────────────────────────────────────────────────────┬────────────────────────────────────────────────────────────┐
│                원인                 │                          문제                           │                            수정                            │
├─────────────────────────────────────┼─────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────┤
│ CSS.Transform.toString(null) →      │ style={{ transform: undefined }}가 서버/클라이언트에서  │ transform·transition 모두 falsy이면 style 객체에서 제외    │
│ undefined                           │ 다르게 직렬화될 수 있음                                 │                                                            │
├─────────────────────────────────────┼─────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────┤
│ attributes 속 aria-describedby      │ DndContext가 주입하는 고유 ID가 SSR·CSR 타이밍 차이로   │ suppressHydrationWarning 추가 — 해당 엘리먼트 한 곳만 경고 │
│                                     │ 불일치                                                  │  억제, 기능 영향 없음                                      │
└─────────────────────────────────────┴─────────────────────────────────────────────────────────┴────────────────────────────────────────────────────────────┘

```


p288 기반 추가 질문
```shell

TicketForm 테스트와 기능 구현이 되어 있습니다. 
아래 내용을 만족하는지 확인해줘.
- src/shared/validations/ticket.ts의 createTicketSchema로 클라이언트 사이드 검증 했는가?  
- form-field, form-imput, form-error 등 globals.css 클래스 활용 활용했는가?
- Phase 1에서 구현한 Button 재사용했는가?


src/client/components 하위 컴포넌트 정의 기준을 설명해줘. 
구현하면서 비게 된 하위 디렉터리를 정리해줘.
```


### 6.4 API 연동과 상태 관리 
p290

```shell
ticketApi 모듈의 테스트를 작성해줘. 아직 구현하지 마.

테스트할 함수: getBoard, create, update, remove, reorder, complete
각 함수별 검증:
- 올바른 HTTP 메서드의 URL로 fetch 호출 
- 요청 body 검증 (POST, PATCH)
- 성공 응답 반환 
- 에러 응답시 error.message throw
  
fetch는 jest.fn()으로 mock 처리해줘.
```

```shell

21개 테스트 Red 상태로 커밋했습니다.

테스트 구성 (함수별 검증 항목):

┌────────────────┬───────────┬──────────────────────────────────────────────────────────────────────┐
│      함수      │ 테스트 수 │                                 검증                                 │
├────────────────┼───────────┼──────────────────────────────────────────────────────────────────────┤
│ fetchBoard     │ 3         │ GET /api/tickets · BoardData 반환 · 에러 throw                       │
├────────────────┼───────────┼──────────────────────────────────────────────────────────────────────┤
│ createTicket   │ 4         │ POST /api/tickets · body 검증 · Ticket 반환 · 에러 throw             │
├────────────────┼───────────┼──────────────────────────────────────────────────────────────────────┤
│ updateTicket   │ 4         │ PATCH /api/tickets/:id · body 검증 · Ticket 반환 · 에러 throw        │
├────────────────┼───────────┼──────────────────────────────────────────────────────────────────────┤
│ deleteTicket   │ 3         │ DELETE /api/tickets/:id · undefined 반환 · 에러 throw                │
├────────────────┼───────────┼──────────────────────────────────────────────────────────────────────┤
│ reorderTicket  │ 4         │ PATCH /api/tickets/reorder · body 검증 · {tickets} 반환 · 에러 throw │
├────────────────┼───────────┼──────────────────────────────────────────────────────────────────────┤
│ completeTicket │ 3         │ PATCH /api/tickets/:id/complete · Ticket 반환 · 에러 throw           │
└────────────────┴───────────┴──────────────────────────────────────────────────────────────────────┘
```


```shell
useTickets Hook을 TDD로 구현해줘

역할: ticketApi를 감싸서 board 상태 관리 + CRUD 제공 
반환: { board, isLoading, error, create, update, remove, reorder, complete }

테스트 검증
- initialData로 board 초기화 
- create/update/remove/reoder/complete 각각 : ticketApi 호출 ->> refreshBoard
- 실패시 error 상태 설정 
- API 호출 중 isLoading=true

ticketApi는 jest.mock으로 처리.. renderHook + act() 사용.
```

```shell
14/14 Green. 커밋 완료.

---
구현 요약:

┌──────────┬────────────────────────────────┬─────────────────────────────┬──────────────┐
│   액션   │        낙관적 업데이트         │          API 호출           │  실패 처리   │
├──────────┼────────────────────────────────┼─────────────────────────────┼──────────────┤
│ create   │ BACKLOG 맨 위에 임시 카드 추가 │ createTicket → fetchBoard   │ 롤백 + error │
├──────────┼────────────────────────────────┼─────────────────────────────┼──────────────┤
│ update   │ 해당 카드 즉시 갱신            │ updateTicket → fetchBoard   │ 롤백 + error │
├──────────┼────────────────────────────────┼─────────────────────────────┼──────────────┤
│ remove   │ 즉시 board에서 제거            │ deleteTicket → fetchBoard   │ 롤백 + error │
├──────────┼────────────────────────────────┼─────────────────────────────┼──────────────┤
│ reorder  │ 대상 컬럼으로 이동             │ reorderTicket → fetchBoard  │ 롤백 + error │
├──────────┼────────────────────────────────┼─────────────────────────────┼──────────────┤
│ complete │ DONE으로 이동                  │ completeTicket → fetchBoard │ 롤백 + error │
└──────────┴────────────────────────────────┴─────────────────────────────┴──────────────┘
```

p292
```shell
# book
Phase 5 의 BoardHeader와 FilterBar를 ..

# me
Phase 4가 다 구현되어 있는지 검토해줘. 
@app/preview/page.tsx 프리뷰 페이지에 추가할 수 있을까?

이제 Phase 5 를 TDD로 구현해줘. 이때 단계별로 커밋을 작성해줘
#실제 page.tsx에서 전체 앱을 확인할 수 있게 되면,
#app/preview/page.tsx 프리뷰 페이지를 삭제해줘

```
- 마찬가지로 도서의 내용과 다르다.. Phase5에 BoardHeader와 FilterBar가 존재하지 않는다 나는

```shell
❯ 프리뷰 페이지가 컴포넌트 UI/UX를 미리보기 용도로 생각됩니다. Phase 4에 hook이나 phase 5 은 BoardContainer나 프리뷰 페이지와 성향이 안 맞지 않나요? 특히 백엔드 api의 경우 swagger 같은 docs로 확인하는게 보통이라고 생각되요

맞습니다. 정확한 지적입니다.

프리뷰 페이지의 본래 목적은 DB 없이 컴포넌트를 시각적으로 확인하는 것 (Storybook 역할)인데, 지금 Phase 4에 성격이 다른 항목들이 섞여 있습니다.

┌──────────────────────────┬─────────────────────┬──────────────────────────────────────────────┐
│           항목           │        성격         │               프리뷰 적합 여부               │
├──────────────────────────┼─────────────────────┼──────────────────────────────────────────────┤
│ ticketApi.ts             │ API 클라이언트 모듈 │ ❌ — 테스트 + Swagger/API 문서               │
├──────────────────────────┼─────────────────────┼──────────────────────────────────────────────┤
│ useTickets               │ 상태 관리 훅        │ ❌ — 단위 테스트로 검증                      │
├──────────────────────────┼─────────────────────┼──────────────────────────────────────────────┤
│ TicketModal              │ UI 컴포넌트         │ ✅ — 시각 확인 필요                          │
├──────────────────────────┼─────────────────────┼──────────────────────────────────────────────┤
│ BoardContainer (Phase 5) │ 통합 컨테이너       │ ❌ — npm run dev → localhost:3000/ 에서 확인 │
├──────────────────────────┼─────────────────────┼──────────────────────────────────────────────┤
│ app/page.tsx (Phase 5)   │ RSC                 │ ❌ — 빌드 + 실제 서버 확인                   │
└──────────────────────────┴─────────────────────┴──────────────────────────────────────────────┘

제안: ticketApi와 useTickets 슬롯을 프리뷰 페이지에서 제거하고, Phase 5도 추가하지 않는 게 깔끔합니다. 프리뷰 페이지는 Phase 1~3 + TicketModal만 담당하고, 나머지는 테스트/실서버로 검증하는 방향이 맞습니다.

제거할까요?

```

```shell
src/client/components/Ticket/TicketDetailView.tsx에 Runtime TypeError 확인했습니다. Done 영역에 DnD 후 상세보기를 하는데 에러가 발생했습니다.
- date.toISOString is not a function 
- 
  

원인 요약:

TypeScript 타입(createdAt: Date)과 JSON 런타임 값(문자열)의 불일치입니다.

API 응답 JSON → fetch().json() → string ("2026-05-31T...")
                                    ↓
TypeScript 타입 → Date          ← 컴파일 타임엔 문제없음
                                    ↓
formatDate(date: Date) → date.toISOString() ← 런타임 TypeError!

useTickets의 낙관적 업데이트는 실제 new Date()를 만들기 때문에 괜찮고, refreshBoard() 이후 API 응답을 받을 때만 터집니다. formatDate가 string도 new Date(string)으로 변환해 처리하도록 수정했습니다.
```


p296
https://github.com/claude-code-expert/todo-app/blob/chapter6-frontend/docs/DESIGN_SYSTEM.md

>[!note] 화면 구성 및 컬러 참고 사이트 모음 
>(1) 웹 및 앱의 디자인 요소와 컬러 참조
>- https://uicolors.app
>- https://www.radix-ui.com
>- https://www.dark.design
>- https://collectui.com
>- https://uibowl.io
>- https://mobbin.com
>- https://scrnshts.club
>(2) 프론트엔드 특화 AI 도구
>- https://v0.app
>- lovable.dev
>- replit.com
>- readdy.ai
>- www.figma.com/ko-kr/make

p294 그대로 복붙
- `docs/DESIGN_SYSTEM.md` : 디자인 가이드라인 (클로드 코드에서 참조만)
- `src/shared/design/colors.json` :  컬러 토큰 
	- 제미나이로 샘플 이미지나 사이트 주소를 리서치해서 colors.json 생성 요청 > 클로드로 다듬기
- https://github.com/claude-code-expert/todo-app/blob/main/src/shared/design/colors.json

p297
```shell
@src/shared/design/colors.json @docs/DESIGN_SYSTEM.md를 우리 사이트에 맞도록 컬러 및 UI를 조정하고 싶어. 
@reference/image/tika-wireframe.png 과 @reference/image/트렐로.png를 참고하고 분석해서 반영해줘 

중요한 점은 4개의 스위밍라인 구분선이 있어야 하고, 각각의 배경색은 달라야 해.
눈에 보기 편한 색이어야 하고 모든 텍스트는 제목 14pt bold, 내용과 인풋 항목은 12pt를 유지하고 input box의 스타일도 일관성 있게 유지해야 해.
```


```shell
적용된 변경 사항을 요약하면:

app/globals.css - 전면 재설계
- body: 라이트 그레이(#F5F6F8) 배경 + display: flex; flex-direction: column; height: 100vh — 스위밍 레인이 전체 높이를 채우도록
- 헤더: 다크 글래스모피즘 → 화이트 플랫(background: #fff; border-bottom)
- 필터바: 투명 다크 버튼 → 라이트 필 버튼 (#f3f4f6 / 활성:#dbeafe)
- 4개 스위밍 레인: 컬럼 간 border-right: 1px solid #D1D5DB 구분선 + 각 컬럼 전체 높이 배경색
  - Backlog: #F0EDFA 라벤더 | accent #8B7EC8
  - TODO: #FFF8E1 옐로 | accent #F59E0B
  - In Progress: #E8F4FD 블루 | accent #3B82F6
  - Done: #E8F5E9 그린 | accent #22C55E
- 컬럼 헤더: 2px 컬러 하단 강조선
- 타이포그래피: 제목 14px bold, 내용/인풋 12px 통일
- 인풋 박스: 모든 form-input 12px, 동일한 border/focus ring 스타일

Column.tsx: STATUS_CLASSES 맵으로 상태별 CSS 클래스 자동 적용
```



p298 6.6.2 디자인 시스템을 CLAUDE.md에 반영하기 
```shell
## 디자인 시스템 
스타일링 작업시 반드시 아래 파일들을 참조할 것:
- 컬러 토큰 (참조): `src/shared/design/colors.json`
- 디자인 가이드: `docs/DESIGN_SYSTEM.md`
- CSS 변수(런타임): `app/globals.css` `:root`

새 컴포넌트 생성시 colors.json의 semantic 컬러와 
DESIGN_SYSTEM.md의 간격/그림자/라운딩 규칙을 따른다.
컬럼 변경시 colors.json과 globals.css의 CSS 변수를 함께 업데이트 한다.
```
- 이 구조의 장점 읽어보기 


디자인 시스템을 적용한 뒤에 메인 화면을 수정한다.
```shell
@src/shared/design/colors.json @docs/DESIGN_SYSTEM.md를 우리 사이트에 맞도록 컬러 및 UI를 조정하고 싶어. @reference/image/tika-wireframe.png 와 @reference/image/트렐로.png를 참고하고 분석해서 반영해줘.

- 4개의 스위밍라인 구분선 필요 
- 각각의 배경색은 달라야 해
- 눈에 보기 편한 색이어야 하고, 모든 텍스트는 제목 14pt bold, 내용과 인풋 항목은 12pt를 유지하고 input box의 스타일도 일관성 있게 유지.
- 메인 화면에서 컨텐츠 영역에 필터 기능이 구현되어 있는데, 이 영역은 좌측의 경우 백로그가 전체 높이에 와야 하고 우측 영역이 상단 필터링 영역(높이 48px)과 하단 3개의 영역으로 나뉘는 구조 


```

```shell
DOM 구조가 정확하게 바뀌었습니다:

변경 전 (FilterBar가 전체 폭 차지):
header.board-header
div.board-filter-bar        ← 전체 너비 (Backlog 위도 포함)
div.board-body
  div.board-sidebar
  div.board-main
    div.board-columns

변경 후 (요구사항대로):
header.board-header
div.board-body
  div.board-sidebar          ← Backlog: 전체 높이 (100vh - 52px)
  div.board-main             ← 우측 패널 (flex-direction: column)
    div.board-filter-bar     ← 필터 영역: 48px (우측만)
    div.board-columns        ← 3개 컬럼: 나머지 높이

수정된 파일 4개:
- Board.tsx: filter?: React.ReactNode prop 추가, board-main 내부에 렌더링
- BoardContainer.tsx: <FilterBar>를 Board의 filter 슬롯으로 이동
- BoardContainer.test.tsx: Board 목이 {props.filter} 렌더링하도록 수정 (기존 필터 테스트 유지)
- globals.css: board-main → flex-direction: column, board-filter-bar → height: 48px
```

p301
>[!tip]
>개인적으로는 HTML을 먼저 뽑아서 데모 화면을 수정하면서, 디자인 시스템과 컬러 파일을 수정한 뒤 화면 구성할 때 HTML을 반드시 참고해서 만들어달라고 지시하는 방식을 선호한다. 
당연히 최소 단위의 컴포넌트들을 먼저 완성하고 별도의 페이지를 만들어둔 뒤 해당 컴포넌트와 데모 HTMl을 지시에 첨부하여 완성도를 높여 나가는게 중요하다


DESIGN_SYSTEM.md, COLOR.json, UI-COMPONENT.md 
- https://github.com/claude-code-expert/tika/tree/develop/docs

> 스타일 수정 요청 예시 참고로 읽기

p303
6.6.3 프론트 개발 플러그인을 활용한 UI 개선 

```shell
# 프런트엔드 디자인 검토 (접근성, 성능, UX)
npx skills add vercel-labs/agent-skilss --skill frontend-design

# React 베스트 프렉티스
npx skills and vercel-labs/agent-skills --skill vercel-recat-best-practices

# 설치된 스킬 확인 
npx skills list
```
- npx ?? 클로드 스킬 등록이 아니고??
- 💩설치 실패해서 우선 생략 