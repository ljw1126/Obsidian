
**엔티티 (Entity)**
- 도메인 모델을 만들 때 사용하는 패턴 
- 도메인 안에 있는 대상이나 개념
- 고유한 식별자(identity)를 가지고 이를 통해서 개별적으로 구분된다
- 📌 `생명주기 (life cycle)`를 가진다. 시간의 흐름에 따라 상태가 변경될 수 있다


> 회원 도메인 모델에 대한 속성(property), 행위(behaivor), 규칙(rule)에 대한 정의 

---

✅ **도메인 모델 패턴**
- 마틴 파울러가 소개한 패턴 
- 도메인/비즈니스 로직을 구성하는 아키텍처 패턴의 한 가지 
- 도메인 모델의 **속성과 행위를 모두 포함**하는 **도메인의 오브젝트 모델**이다
- 오브젝트 모델이기 때문에 **복잡한 연관 관계, 커스텀 속성, 상속** 등을 사용할 수 있다.
- 참고.
	- 트랜잭션 스크립트는 하나의 업무 절차를 처리하기 위한 스크립트를 만들고, 비즈니스 로직을 순서대로 코드로 작성하는 방법
		- 도메인 모델 패턴의 장점이 있다.

---

```java
Objects.requireNonNull(..);
```


> 🔎 xunit test snippet 찾아보기, rider test template 
- https://xunit.net/docs/config-xunit-runner-json
- https://www.jetbrains.com/help/rider/Reference__Templates_Explorer__File_Templates_Test.html

> 기술 의존적이라는 생각이 들 수 있지만 유틸이라 상관 없다. 나중에 외부에서 주입받는 형태로 하면

> 개행 문자를 통해 문맥을 구분하는것도 좋다


---

JSR 305 : Annotations for Software Defect Detection 
- 정식 스펙으로 허용 x

> gradle plugins 사이트 검색 
- `com.github.spotbugs` 의존성 추가

---

### 도메인 모델 확장하기

✅ **도메인 서비스**
- 특정 엔티티에 넣기 애매한, 상세 구현을 뽑아냄 
- 도메인 밖에서
	- 인터페이스이기때문에 행위만 있으면 된다
		- encode
		- matche 


인터페이스부터 정의 
테스트에서는 익명 클래스로 (추상 메서드가 2개라서)

---

### Member 도메인 코드 개선

정적 팩터리 메서드 -> 빌더 패턴 -> 빌더 패턴 단점 -> 내부적으로 private 생성자로 setter 할당 방식 사용 


---

### 도메인 모델의 값 객체 도입 

이메일 형식이 틀린 경우에 대해 
- 예외 단위 테스트 부터 작성함 (실패 확인)
- VO를 도입하신다고 함 

**값 객체 (value object)**

> 식별자로 구분 가능한 경우 엔티티로 만들면 된다 

- 도메인 모델에서 식별자가 필요하지 않고 속성/값으로만 구별되는 오브젝트 
- 엔티티가 너무 많은 책임을 가지는 것을 방지하고 특정 속성 관련 행위를 분리해서 엔티티를 더 집중된 상태로 유지하게 한다. 
- 원시 타입보다는 도메인 개념을 더 명시덕으로 나타내서 모델의 명확성을 높인다
- 생성 이후에 상태가 변하지 않고 변경이 필요하면 새로운 객체로 교체한다. 
- 풍부한 기능을 가진다
- 자체 유효성 검사도 가능하다 


**원시 타입을 객체로 교체 - 리팩터링** 
- 원시 타입의 남용을 개선하고 코드의 명시성과 안정성을 높이는 리팩터링 방법
- 값 객체는 **도메인 모델**의 핵심 **구성 요소**로 **값 그 자체의 의미를 담아내고 불변성, 동등성** 등의 도메인 로직을 포함하여 설계하는 방식


---

### 도메인 모델 패턴 
> 그리고 애플리케이션 서비스를 Domain Facade라고도 부른다

> 첫 단계로 속성과 관련있는 도메인 로직, 행위를 같이 넣어둔다. (제한 걸기)


**도메인 모델 패턴의 두가지 스타일** 
- `단순 도메인 도멜 (SImple Domain Model)`
	- DB 설계와 유사, 테이블에 대해 하나의 도메인 오브젝트 
	- 빈약한 도메인 모델 (트랜잭션 스크립트마냥 서비스에 노출됨)
- `풍성한 도메인 모델 (Rich Domain Model)`
	- 상속, 전략, GoF 디자인 패턴, 연관 관계
	- 복잡한 로직에 적합하지만 DB 매핑이 어려울 수 있다.

**도메인 로직의 API 개발**
- Application Programming Interface
- 도메인 모델 패턴은 트랜잭션 스크립트 처럼 *작업 단위의 절차형 API*를 만들기 어렵다 (긍정적인 부분)
- 도메인 로직의 명확한 `작업 단위 API`를 제공하는 `애플리케이션 서비스`가 필요
	- 애플리케이션 서비스를 `Domain Facade` 라고도 부른다 

---

### Quiz

```text
Q. 도메인 모델에서 도메인 서비스(Domain Service)는 어떤 역할을 수행할 때 주로 사용될까요?

A. 여러 도메인 객체 관련 복잡 로직 처리 
도메인 서비스는 특정 엔티티나 값 객체에 속하기 어려운 로직, 즉 여러 도메인 객체를 조율하거나 외부 시스템과 통합하는 복잡한 도메인 작업을 처리해요.


Q. Java 코드에서 @NonNull 같은 어노테이션과 SpotBugs 같은 정적 분석 도구는 널(null) 안정성 확보에 어떻게 기여하나요?

A. 빌드 시점에 널 관련 문제 발견 
@NonNull 어노테이션으로 널 허용 여부를 표시하고, SpotBugs 같은 도구는 이를 분석하여 코드가 널 제약을 위반하는 부분을 컴파일/빌드 시점에 미리 찾아내 줍니다.


Q. 빈약한 도메인 모델(Anemic Domain Model)의 특징은 무엇일까요?

A. 데이터만 가지고 있고 행위 로직은 외부에 위치 
빈약한 도메인 모델은 속성(데이터)만 가지고 있고, 그 데이터를 처리하는 비즈니스 로직은 서비스 계층과 같이 객체 외부에 분산된 형태를 말해요.


Q. 도메인 모델 패턴을 사용하는 시스템에서 애플리케이션 서비스 계층의 주요 역할은 무엇인가요?

A. 사용자 요청에 따른 작업 실행 조율 
애플리케이션 서비스는 도메인 모델의 기능을 조합하여 사용자나 외부 시스템의 '요청(작업)'을 처리하는 역할이에요. 도메인 로직 자체는 도메인 계층에 있죠.


```