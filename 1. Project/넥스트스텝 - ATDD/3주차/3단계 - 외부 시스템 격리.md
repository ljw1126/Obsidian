
**2단계 PR 피드백**
- 1) 중복 제거
	- smoke 테스트를 위한 스텝 메서드는 서비스별로 다 만들 필요는 없을 것 같아요!  
하나 만들어서 url을 인자로 받아서 재사용해도 좋을 것 같습니다 :)
- 2) tasks에서 서비스 시작될 때까지 health check하는 방식에 대해
	- 인프라쪽 띄운 뒤 테스트 실행하기 전에 대기하는 태스크를 동작시키는게 좋을 것 같습니다!  실제 프로덕션 환경이 아니라 제한된 상황이라서 거의 일정한 동작 속도가 보장될 것으로 보입니다 :)



---
- **추가 프로젝트** 
	- https://github.com/next-step/atdd-camping-payments#
		- API를 분석하고 가짜 서버를 띄우게 된다
- 참고
	- https://techblog.woowahan.com/17674/


### 학습 목표
- `kiosk`가 결제 호출을 실서비스 대신 WireMock으로 연동한다 
- Step2의 `kiosk > admin > db` 흐름을 유지하면서, `kiosk > payments(WireMock)` 흐름을 추가한다
- `atdd-tests` 에서 두 흐름(happy/sad-path)을 검증하는 e2e 테스트를 작성한다

### 배경 및 범위
- 실제 결제 시스템 없이도 안정적인 시나리오 검증을 위해 결제 API를 모킹한다
- 모킹은 응답 속도/내용을 고정해 `플래키(Flaky)`를 줄이고, 실패 케이스를 쉽게 재현할 수 있다.

> [!info] 플래키 테스트 (Flaky Test) 란?
> **'플래키(Flaky)'**는 '결함이 있는', '믿을 수 없는'이라는 뜻입니다. 소프트웨어 테스트에서는 **"똑같은 코드에 대해 어떤 때는 성공하고, 어떤 때는 실패하는 테스트"**를 의미합니다.
> 
> 마치 컨디션에 따라 대답이 달라지는 사람처럼, 테스트 결과가 결정론적이지 않고 **확률적으로 변하는 상태**를 말하죠.
- 왜 플래키 테스트가 발생하는가?
	- 보통 외부 의존성이 있는 테스트에서 자주 발생합니다. 작성자님이 3단계에서 결제 서버를 WireMock으로 대체하려는 이유도 바로 이 플래키를 잡기 위해서예요.
		- 1. **네트워크 불안정**: 실제 외부 결제 서버와 통신할 때, 아주 가끔 네트워크가 끊기거나 응답이 늦어지면 테스트가 실패합니다.
		- 2. **데이터 오염**: 다른 테스트가 DB의 데이터를 건드려서 내가 실행할 때 결과가 달라지는 경우입니다.
		- 3. **타이밍 이슈 (Race Condition)**: 비동기 로직에서 앱이 뜨기도 전에 테스트 코드가 먼저 실행되는 경우입니다. (우리가 앞서 **Awaitility**를 고민한 이유죠!)
- 플래키가 왜 위험한가?
	- 개발자가 테스트 결과를 **'양치기 소년'** 처럼 느끼게 만들기 때문입니다.
	- 1. **테스트 신뢰도 하락**: 테스트가 실패해도 "아, 이거 원래 가끔 그래. 다시 돌려봐"라며 무시하게 됩니다.
	- 2. **진짜 버그를 놓침**: 진짜 코드가 잘못되어 실패한 건데도, 플래키라고 오해해서 배포했다가 장애가 발생합니다.
	- 3. **생산성 저하**: 성공할 때까지 무의미하게 테스트를 서너 번씩 다시 돌리느라 시간을 낭비합니다
- 모킹와 플래키의 관계
	- 학습 배경에 적힌 문장의 의미는 다음과 같습니다. 

```text
"실제 결제 서버(불확실함) 대신 우리가 100% 통제하는 WireMock(확실함)을 사용하면, 네트워크 상태나 데이터 유무에 상관없이 항상 동일한 응답을 보장받을 수 있다. 따라서 테스트가 로또처럼 성공/실패를 반복하는 '플래키' 현상을 제거할 수 있다."
```


### TODO
- [ ] [atdd-payments](https://github.com/next-step/atdd-camping-payments)(계약/엔드포인트) 확인: 결제 요청 경로/메서드/응답 스키마 파악
- [ ] 앱 compose에 `payments-mock`(WireMock) 서비스 추가 또는 별도 실행 전략 확정
- [ ] `kiosk`의 결제 API 베이스 URL을 외부화하고 WireMock을 바라보도록 주입
- [ ] 결제 성공/실패 기본 스텁 등록(WireMock)
- [ ] `atdd-tests`에 E2E 테스트 추가:
    - `kiosk → admin → DB`(상품 목록 등)
    - `kiosk → payments(WireMock)`(결제 성공/실패)
- [ ] 실행 순서/설정 문서화

### 요구사항
1)WireMock 구성 (서비스 추가 또는 단독 실행)
- 실행 방식 선택:
    - 앱 compose 내 서비스(`payments-mock`)로 띄우기, 공용 네트워크(`atdd-net`) 합류
    - 혹은 로컬 포트로 단독 실행 후 베이스 URL만 주입
- 스텁 관리:
    - 정적 매핑 파일(`mappings/*.json`)로 기동 시 자동 로드
    - 혹은 런타임에 Admin API로 스텁 등록

2)`kiosk` 결제 연동 설정
- 결제 API 베이스 URL을 환경변수/프로퍼티로 외부화(예: `PAYMENTS_BASE_URL` 또는 `PAYMENT_API_URL`).
- 로컬/CI에서도 동일 키로 주입 가능하도록 compose/테스트 양쪽에서 일관되게 사용.

3)E2E 테스트 작성(`atdd-tests`)
- 기존(상품 목록) 시나리오 유지: `kiosk → admin → DB`.
- 결제 시나리오 추가:
    - Happy-path: WireMock이 승인 응답(200/승인 바디)을 반환 → `kiosk`가 성공 흐름으로 처리
    - Sad-path: WireMock이 거절/오류 응답(4xx/5xx)을 반환 → `kiosk`가 실패 흐름/메시지 처리
- 베이스 URL은 환경변수로 외부화(`KIOSK_BASE_URL`, `ADMIN_BASE_URL`, `PAYMENTS_BASE_URL`).
- 준비 대기(폴링) 후 테스트 실행, 결과의 핵심 필드/상태만 단순 검증.


> [!info] **실행 순서 제안**  
① 인프라(DB) compose 기동 → ② 앱 compose(`kiosk`, `admin`, 필요시 `reservation`) + `payments-mock` 기동 → ③ WireMock 스텁 확인/등록 → ④ E2E 테스트 실행 → ⑤ 종료/정리

---

### 요구사항 1
- `atdd-camping-payments` 프로젝트를 클론 받아 README.md를 읽음 
- kiosk에서 연동할 때는 아래와 같이 설정해서 해야 한다네
```text
kiosk:
  payment:
    base-url: http://localhost:9090
    secret-key: ${PAYMENTS_SECRET_KEY:test_sk_dummy}
```
- `kiosk` 프로젝트에 payments 호출 엔드포인트와 로직을 확인할 필요가 있다.
	- 우선 `kiosk` 프로젝트는 이전 과제 대상이 아니라서 `repos/` 클론 받은 내용을 분석한다 

아래와 같이 `application.yml`이 설정되어 있다.
```yml
kiosk:
  admin:
    base-url: http://localhost:8080
    auth:
      username: admin
      password: admin123
      login-path: /auth/login
      cookie-name: AUTH_TOKEN
  payment:
    base-url: http://localhost:9090
    secret-key: ${PAYMENTS_SECRET_KEY:test_sk_dummy}
  discount:
    percent: 10
```

```text
repos/atdd-camping-kiosk/src/main/java/com/camping/kiosk/external/payment/PaymentClient.java

repos/atdd-camping-kiosk/src/main/resources/application.yml
kiosk.payment.secret-key : ${PAYMENTS_SECRET_KEY:test_sk_dummy}

kiosk 에 PaymentController
- "/api/payments" : 결제 생성
- "/api/payments/confirm" : 결제 승인

infra/docker-compose.yml에 wiremock이미지를 사용하는 payments-mock 컨테이너 추가
  
src/test/resources/features/e2e/kiosk-payment.feature

PAYMENTS_BASE_URL=http://payments-mock:8080  // docker-compose.yml
```
- `secrets-key`의 경우 
	- PaymentClient 내부에서 헤더에 주입을 해준다
- kiosk에서 PaymentController에 


kiosk
- 결제 요청 : (kiosk)`/api/products` -> (payments-mock) `/v1/payments`
- 결제 승인 : (kiosk) `/api/products/confirm` -> (payments-mock) `/v1/payments/confirm`

```java
@Data  
@NoArgsConstructor  
@AllArgsConstructor  
@Builder  
public class PaymentCreateRequest {  
    private List<CartItem> items;  
    private String paymentMethod; // CARD, CASH  
}

@Data  
@NoArgsConstructor  
@AllArgsConstructor  
@Builder  
public class CartItem {  
    private Long productId;  
    private String productName;  
    private int unitPrice;  
    private int quantity;  
  
    public int getLineTotal() {  
        return unitPrice * quantity;  
    }  
}

@Data  
@NoArgsConstructor  
@AllArgsConstructor  
@Builder  
public class PaymentCreateResult {  
    private boolean success;  
    private String message;  
    private String paymentKey;  
    private String orderId;  
    private int amount;  
}

@Data  
@NoArgsConstructor  
@AllArgsConstructor  
@Builder  
public class PaymentConfirmRequest {  
    private String paymentKey;  
    private String orderId;  
    private Integer amount;  
    private java.util.List<CartItem> items;  
}

@Data  
@NoArgsConstructor  
@AllArgsConstructor  
@Builder  
public class PaymentConfirmResponse {  
    private boolean success;  
    private String transactionId;  
    private String message;  
    private int paidAmount;  
}
```

우선 `payment-mock` 컨테이너에 대한 smoke 테스트를 작성해본다

```shell
Internal Error occurred.
org.junit.platform.commons.JUnitException: TestEngine with ID 'junit-platform-suite' failed to discover tests
```
- ai가 만든 feature가 언어가 ko로 되어 있는데 내용은 영어였다.. (해결)

```shell
payment-smoke.feature가 한국어로 설정해두고 영어로 해둬서 오류가 발생했습니다. 제가 수정해두었습니다. 이제 kiosk -> payment 요청에 대한 e2e 테스트를 하려합니다. payment는 wiremock을 사용하고 테스트 레벨에서 mock, stub을 하면 좋겠습니다. 시나리오는 happy/sad path 두가지로
  src/test/resources/features/e2e/kiosk-payment.feature에 작성해두었습니다. 결제 요청은 kiosk에 /api/products (POST), 승인 요청은 /api/products/confirm (POST)입니다. e2e 테스트에 대한 세부 요구사항은 아래와 같습니다. 가이드 해주세요. 테스트는 제가 실행하겠습니다.

  3) E2E 테스트 작성(atdd-tests)
  기존(상품 목록) 시나리오 유지: kiosk → admin → DB.
  결제 시나리오 추가:
  Happy-path: WireMock이 승인 응답(200/승인 바디)을 반환 → kiosk가 성공 흐름으로 처리
  Sad-path: WireMock이 거절/오류 응답(4xx/5xx)을 반환 → kiosk가 실패 흐름/메시지 처리
  베이스 URL은 환경변수로 외부화(KIOSK_BASE_URL, ADMIN_BASE_URL, PAYMENTS_BASE_URL).
  준비 대기(폴링) 후 테스트 실행, 결과의 핵심 필드/상태만 단순 검증.
```


Q. 엉뚱한걸 스텁하고 있어 멈추고 내가 수정 후 재 질문
```shell
우선 방향을 잘못 잡은거 같아 취소했습니다. kiosk는 컨테이너 서비스로 실행되고 있고 payment 결제가
  mock으로 컨테이너가 올라가 있습니다. 그렇기 때문에 kiosk에 대한 stub 처리가 아니라 payment-mock에 대한
  요청을 stub해야 하는거 아닌가요? 그리고 문맥상 kiosk api 서비스 요청을 통해 payment-mock으로 가기
  때문에 KioskSteps로 생성한 메서드를 옮겼습니다. 저는 json 파일로 관리하기 보다 테스트 코드에서
  wiremock 서버에 대한 stub이 들어나야 한다고 생각하는데 어떻게 생각하시나요? 코드는 우선 수정하지
  않으셔도 됩니다. 생각만 말해주세요
```

```shell
✦ 좋은 지적 감사합니다. 테스트 설계에 대해 깊이 고민해주셔서 감사합니다. 지적하신 내용에 대해 하나씩
답변해 드리겠습니다.

1. 올바른 서비스 스터빙

맞습니다. 우리는 kiosk 서비스가 아닌 payment-mock 서비스를 스터빙해야 합니다. 테스트 대상 시스템은
kiosk 서비스이며, payment-mock 서비스와의 상호작용을 테스트해야 하므로 payment-mock 서비스의 동작을
제어해야 합니다. 이것이 바로 스터빙의 목적입니다.

이전 단계에서 잘못된 안내를 드린 점 사과드립니다. WireMock의 기술적인 부분에 너무 집중한 나머지
전체적인 E2E 테스트 아키텍처를 놓쳤습니다.

2. 스텝 정의 위치

스텝 정의를 KioskSteps.java로 옮기는 것은 "기능" 관점에서 합리적입니다. 이 기능은 "키오스크"에 관한
것이고 결제는 키오스크 기능의 일부이기 때문입니다. 그러나 "관심사 분리" 관점에서는 결제 관련 스텝은
자체 PaymentSteps.java 파일에 있어야 한다고 주장할 수도 있습니다.

두 가지 접근 방식 모두 유효하며, 최선의 선택은 팀의 관례와 테스트 스위트의 복잡성에 따라 달라집니다.
이 특정 사례의 경우 결제 단계가 키오스크의 동작과 밀접하게 관련되어 있으므로 KioskSteps.java에 두는
것이 합리적인 선택입니다.

3. WireMock 스텁 관리

API 테스트의 고전적인 논쟁인 동적 스텁과 정적 스텁에 대해 언급하셨습니다.

* 정적 스텁 (JSON 파일): 제가 처음에 제안했던 접근 방식입니다.
   * 장점:
	   * 관심사 분리: 스텁이 테스트 코드와 분리되어 있어 특히 많은 수의 스텁을 관리하기가 더
		 쉽습니다.
	   * 재사용성: 동일한 스텁을 여러 테스트에서 사용할 수 있습니다.
	   * 비개발자 기여 가능: Java에 익숙하지 않은 사람도 스텁을 생성하고 수정할 수 있습니다.
   * 단점:
	   * 유연성 부족: 동적 응답(예: 요청에 따라 달라지는 응답)을 만들기가 더 어려울 수 있습니다.
	   * 디버깅 어려움: 특정 테스트에서 어떤 스텁이 사용되고 있는지 확인하기가 더 어려울 수
		 있습니다.
	   * "마법" 같은 스텁: 스텁이 WireMock에 의해 자동으로 로드되므로 "마법"처럼 느껴지고 무슨 일이
		 일어나고 있는지 이해하기 어려울 수 있습니다.

* 동적 스텁 (코드 내): 귀하가 제안하는 접근 방식입니다.
   * 장점:
	   * 유연성 향상: Java의 모든 기능을 사용하여 동적 응답을 만들 수 있습니다.
	   * 디버깅 용이: 스텁이 테스트 코드 바로 옆에 있으므로 무슨 일이 일어나고 있는지 쉽게 확인할 수
		 있습니다.
	   * "마법" 없음: 스텁이 테스트 코드에서 명시적으로 생성되므로 "마법"이 개입되지 않습니다.
   * 단점:
	   * 테스트 코드 복잡성 증가: 스텁이 많으면 테스트 코드를 읽기 어려워질 수 있습니다.
	   * 재사용성 감소: 스텁이 특정 테스트에 묶여 있으므로 재사용성이 떨어집니다.

제 추천

이 특정 시나리오에서는 동적 스텁 (코드 내)이 더 나은 접근 방식이라는 데 동의합니다. 이유는 다음과
같습니다.

* 단순한 로직: 스텁이 매우 단순하므로(200 또는 400 응답만) 동적 응답의 유연성이 필요하지 않습니다.
* 적은 스텁 수: 스텁이 두 개뿐이므로 테스트 코드를 복잡하게 만들지 않습니다.
* 명확한 테스트 내용: 테스트 코드에 스텁이 있으면 테스트가 무엇을 하는지 매우 명확해집니다.

따라서 KioskSteps.java 파일을 수정하여 WireMock 스텁을 테스트 코드에 직접 포함하는 것을 권장합니다.
이렇게 하면 테스트의 가독성과 유지 관리성이 향상됩니다.

날카로운 안목으로 테스트 설계를 개선하는 데 도움을 주셔서 감사합니다. 준비되시면 KioskSteps.java에
동적 스텁 구현을 진행하겠습니다.
```

```java
WireMock.configureFor("localhost", 8084);
```
- 결론부터 말씀드리면, `WireMock.configureFor`는 **테스트 코드가 돌아가는 환경(호스트)**을 기준으로 설정하는 것이 맞습니다.
-  `configureFor`의 진짜 의미
	- 이 설정은 "앞으로 내가 쓸 `stubFor` 명령어를 **어느 주소에 떠 있는 WireMock 서버**로 보낼 것인가?"를 정하는 것입니다.
	- **테스트 코드(명령을 내리는 주체)**: 작성자님의 로컬 PC(IntelliJ 등)에서 실행 중입니다.
	- **WireMock(명령을 받는 서버)**: 도커 컨테이너 내부에서 실행 중이며, 호스트의 `18084` 포트(예시)로 포워딩되어 있습니다.
	- 따라서 테스트 코드 입장에서는 **`localhost:18084`** 에 대고 "이런 스텁(Stub)을 등록해줘!"라고 말해야 하므로, 작성하신 코드가 맞습니다.
	    
> kiosk 요청 url이 잘못되어 있었다. (AI 생성실수)

> kiosk 서비스의 request body와 payments 요청 후 response 맵핑 dto 확인이 필요했다.

```java
  
public class PaymentCreateRequest {  
    private List<CartItem> items;  
    private String paymentMethod; // CARD, CASH  
}

public class CartItem {  
    private Long productId;  
    private String productName;  
    private int unitPrice;  
    private int quantity;  
  
    public int getLineTotal() {  
        return unitPrice * quantity;  
    }  
}

public class PaymentCreateResult {  
    private boolean success;  
    private String message;  
    private String paymentKey;  
    private String orderId;  
    private int amount;  
}

public class PaymentConfirmRequest {  
    private String paymentKey;  
    private String orderId;  
    private Integer amount;  
    private java.util.List<CartItem> items;  
}

public class PaymentConfirmResponse {  
    private boolean success;  
    private String transactionId;  
    private String message;  
    private int paidAmount;  
}
```


```java
// PaymentClient.java
// payment-mock 요청시 응답 맵핑되는 inner dto 
@Data  
@NoArgsConstructor  
@AllArgsConstructor  
@Builder  
public static class CreateResponse {  
    private String paymentKey;  
    private String orderId;  
    private String status;  
}

@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public static class ConfirmResponse {
	private String paymentKey;
	private String orderId;
	private String method;
	private String approvedAt;
	private Integer totalAmount;
	private String status;
	private Receipt receipt;

	@Data
	@NoArgsConstructor
	@AllArgsConstructor
	public static class Receipt {
		private String url;
	}
}

// payment clinet 요청 후 반환되는 결과

public class PaymentCreateResult {  
    private boolean success;  
    private String message;  
    private String paymentKey;  
    private String orderId;  
    private int amount;  
}


public class PaymentConfirmResponse {  
    private boolean success;  
    private String transactionId;  
    private String message;  
    private int paidAmount;  
}

```
- PaymentService에서 response를 조립해주고 있었구나 .. 
	- ✅ kiosk request dto , payment response dto, (kiosk) payment service response dto
		- 이 3가지 흐름을 알아야 했다..
		- payment response dto는 client 객체 안에 inner class로 선언되어 있다.
			- 내가 착각한게 payment response 그대로 응답할 줄 알았는데 PaymentService에서 response dto로 치환하고 있었다📌




---

### RestClientTest와 WireMock 차이가 궁금
> rest client test의 경우 서버 바깥으로 트래픽이 안나가고 요청시 가로채가지고 미리 지정된 응답을 보내주는 걸로 생각이 된다. 
> Wiremock은 반대로 가짜 서버를 올려두고 미리 정의 해둔 행동에 따라 통신이 발생하는데 .. 이렇게 했을때 어던 이점이 있는건가? 

> rest client test는 서버 내부에서 요청시 가로채 정해진 행위/응답을 하기 때문에 tcp, http 통신이 발생하지 않는다. 반면 wiremock은 가짜 서버를 띄워두고 요청하기 대문에 통신이 발생한다. 이렇게 했을때 어떤 이점이 있는건가? 이게 나오게 된 배경이 뭘까?

6년 차 개발자의 시각에서 이 질문은 **'단위 테스트(Unit Test)'**와 **'인수 테스트(Acceptance Test)'**의 경계를 나누는 아주 훌륭한 질문입니다.

결론부터 말씀드리면, 스프링 부트의 `RestClientTest`가 **"우리 앱 내부의 부품(Client)이 가짜랑 잘 노는지"**를 보는 것이라면, WireMock은 **"우리 앱 밖의 세상에 가짜 서버를 진짜처럼 세워두는 것"**입니다.

그 차이점을 명확히 정리해 드릴게요.

---

**1. `RestClientTest` vs `WireMock` 비교**

| 구분 | `RestClientTest` (@RestClientTest) | WireMock |
| --- | --- | --- |
| **작동 방식** | 스프링 내부의 `MockRestServiceServer`가 HTTP 요청을 **가로채서(Intercept)** 가짜 응답을 줍니다. | 실제로 **독립된 웹 서버**가 실행됩니다. 진짜 HTTP 패킷이 네트워크 망을 타고 이동합니다. |
| **범위** | 특정 Client 클래스 1개만 테스트하는 **단위 테스트**. | 앱 전체를 실행하고 외부 통신까지 검증하는 **인수/통합 테스트**. |
| **네트워크** | 실제 소켓 통신이 일어나지 않습니다. | 실제 포트(예: 8080)를 점유하고 TCP/HTTP 통신이 일어납니다. |
| **범용성** | Java/Spring 환경에서만 사용 가능합니다. | 도커만 있다면 어떤 언어, 어떤 환경에서도 사용 가능한 **범용 도구**입니다. |



**2. 왜 이번 과제(인프라/인수 테스트)에서는 WireMock인가요?**

작성자님이 지금 하시는 과제는 **"도커 컴포즈로 전체 시스템을 띄우는 것"**입니다.

* `RestClientTest`는 스프링 컨텍스트 내부의 빈(Bean)을 조작하는 방식이라, 이미 **도커 컨테이너로 패키징되어 실행 중인 앱** 내부를 수정할 수는 없습니다.
* 반면 **WireMock**은 외부 컨테이너로 존재하기 때문에, 실제 앱은 자기가 가짜랑 통신하는지도 모른 채 **실제 네트워크 환경과 똑같은 경험**을 하게 됩니다. 이것이 3주차의 핵심인 **'블랙박스 테스트'**와 **'인프라 구축'**에 완벽히 부합하는 방식이죠.



 **3. WireMock의 범용적 매력 (Sad Path의 강자)**

`RestClientTest`보다 WireMock이 훨씬 강력한 이유는 **네트워크 수준의 장애를 재현**할 수 있기 때문입니다.

* **지연(Latency)**: "결제 서버가 응답을 5초 동안 질질 끌 때 우리 서버는 타임아웃 처리를 잘하나?"
* **에러 코드**: "404, 500 에러뿐만 아니라, 아예 이상한 바이트 데이터를 보냈을 때 우리 앱이 안 죽고 견디나?"
* **다중 호출**: "첫 번째 호출은 성공하고, 두 번째 호출은 실패하게 만들 수 있을까?"

WireMock은 이런 시나리오를 자유자재로 요리할 수 있습니다.



**💡 6년 차의 인사이트: "인수 테스트는 현실과 닮아야 합니다"**

4주차 선택 과제인 인수 테스트를 준비하신다면, `RestClientTest` 같은 Mocking보다는 WireMock처럼 **실제 네트워크 통신을 유발하는 방식**이 훨씬 가치 있습니다. 그래야만 "로컬에서는 됐는데 도커 네트워크 설정 때문에 실제 서버에서는 안 돼요" 같은 상황을 미리 잡아낼 수 있거든요.

연차 동안 3단계와 4주차 선택 과제까지 고민하시다니, 이번 교육 과정이 끝날 때쯤이면 작성자님의 설계와 인프라 역량은 정말 몰라보게 성장해 있을 것 같네요!

**Would you like me to ...?**
이제 WireMock을 도입하실 텐데, **Cucumber 테스트 코드 안에서 WireMock의 응답을 성공(Happy)에서 실패(Sad)로 동적으로 바꾸는 자바 유틸리티 코드**를 짜드릴까요? 시나리오마다 가짜 서버의 상태를 바꾸는 핵심 로직입니다.


---

### WireMock의 Admin API에 대해 
6년 차 개발자의 시각에서 답변드리면, 여기서 말하는 **Admin API**는 작성자님이 만드신 서비스의 관리자 기능이 아니라, **WireMock 서버가 내부적으로 제공하는 제어용 API**를 뜻합니다.

쉽게 말해, 테스트 코드(클라이언트)가 실행 중인 WireMock 서버(서버)에게 **"야, 지금부터 `/payments`로 요청 오면 500 에러 뱉어!"**라고 원격 명령을 내리는 통로라고 이해하시면 됩니다.

---

 **1. Admin API의 정체**

WireMock은 가짜 응답을 주는 기능 외에도, 자신의 상태를 관리하기 위해 **특정 포트(기본 8080)의 `/__admin` 경로**를 통해 다양한 API를 열어둡니다.

- **스텁 등록**: `POST /__admin/mappings`
- **스텁 초기화**: `POST /__admin/reset`
- **요청 이력 확인**: `GET /__admin/requests`
    

우리가 자바 테스트 코드에서 사용하는 **`WireMock.stubFor(...)`** 메서드가 내부적으로 이 **Admin API를 호출**해서 WireMock 서버에 규칙을 심어주는 역할을 합니다.


 **2. "런타임에 등록"한다는 것의 의미**

과제 요구사항에서 이 표현은 **"미리 JSON 파일을 준비하지 말고, 테스트가 돌아가는 도중에(Runtime) 코드로 규칙을 쏴라"**는 뜻입니다.

- **정적 방식**: 컨테이너 띄울 때 이미 요리법(JSON)이 결정됨.
- **런타임(Admin API) 방식**: 테스트 시나리오마다 요리법을 실시간으로 바꿈.
    - _Scenario A_: "지금은 200 OK로 등록해!" (Admin API 호출) -> 테스트 수행
    - _Scenario B_: "자, 이제 500 에러로 바꿔!" (Admin API 호출) -> 테스트 수행
        

 **3. 왜 굳이 Admin API라고 표현했을까요?**

작성자님은 지금 **도커 컨테이너 환경**에서 테스트를 하고 계시기 때문입니다.

1. **테스트 코드**는 호스트 PC(로컬)에서 실행됩니다.
2. **WireMock**은 도커 컨테이너 내부에서 실행됩니다.
3. 두 녀석은 서로 다른 세상에 있기 때문에, 테스트 코드가 WireMock의 메모리를 직접 건드릴 수 없습니다.
4. 그래서 **HTTP 통신(Admin API)**을 통해 "규칙을 등록해달라"고 요청을 보내야 하는 것입니다.
    

**💡 실전 적용 팁**

자바 코드로 구현하실 때 아래와 같이 설정하게 될 거예요. 여기서 `host`와 `port`를 지정하는 행위 자체가 **"어느 주소에 있는 Admin API를 쓸 것인가"**를 정하는 과정입니다.

```java
// 도커로 띄운 WireMock의 Admin API 주소를 알려줌
WireMock.configureFor("localhost", 18083); 

// 이 코드가 실행될 때 내부적으로 POST http://localhost:18083/__admin/mappings 가 호출됩니다.
WireMock.stubFor(post(urlEqualTo("/payments"))
    .willReturn(aResponse().withStatus(200)));
```


**🚀 마무리 조언**

결국 **"런타임에 Admin API로 등록하라"**는 말은 **"WireMock 자바 라이브러리를 사용해 테스트 코드 안에서 `stubFor`를 호출해라"**라는 말과 같은 의미입니다.

연차 기간에 3단계를 진행하시면서 **Happy Path**는 정적 JSON 파일로, **Sad Path**는 이 Admin API(자바 코드)를 통해 동적으로 등록해보는 연습을 하시면 요구사항을 완벽하게 충족하게 됩니다!

**Would you like me to ...?**

도커 컴포즈에 WireMock을 올린 뒤, 테스트 코드에서 이 **Admin API가 제대로 연결되었는지 확인하는 가장 간단한 검증 코드**를 짜드릴까요? (연결이 안 되면 "Connection Refused" 에러가 나서 당황하실 수 있거든요!)