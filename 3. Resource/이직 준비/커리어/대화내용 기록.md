
**LoggerData 내부 로직에 대해 질문했을때**
- `(병건, 팀장님 모두)"로거는 10분 통계를 구하고, 그 10분 통계로 1시간 통계를 구합니다. 그리고 그 1시간 통계로 하루 통계를 구하구요"` 

**LoggerData 리팩터링 1차 코드 리뷰 당일** 
- 갓 클래스에 있는 하드 코딩된 코드를 풀기 위해 서비스와 레파지토리로 분리 
- 피드백 없음 
- `(팀장님)"누구 보다 로거에 대해 잘 알아야 하고, 누구보다 로거에 대해 잘 설명할 수 있어야 합니다."`

**LoggerData 리팩터링 2차 코드 리뷰 전**
- `(기획 팀원 두명) "백엔드팀에 로거 내부 로직 설명 가능한 사람이 한명도 없다"`
	- 용한님 옆에서 눈치 살피는 모습 봄 (이미 다 알고 있었는데)

**LoggerData 리팩터링 2차 코드 리뷰 당일**
- `(팀장님) "... (팀원들에게) 이렇게 하면 됩니다."`
- 질문 없는지 요청했을때 성준님만 질문함
	- 질문1. 인터페이스 분리에 관해
	- 질문2. 변수 선언 위치에 관해
- 코드 리뷰 마치고 `(팀장님) "ㅋㅋ.. 저는 채팅으로 피드백 보낼게요."`
	- 성준님 질문한 내용 그대로 복사 붙여넣기 였음 (스크린샷 첨부)


**CCTV Analysis 인수인계**
- 플래그 변수에 관해 
	- `(민섭님) "음.. 이걸 어떻게 처리해야 할지 모르겠다"`
	- `(저) "플래그 비트는 두 가지 분기로 나눠지게 되는데, 내부 로직을 볼 수 밖에 없게 만듭니다. 플래그 비트 분기에 따라 메서드를 두 개로 나누고 호출부(클라이언트) 코드에서 분기별로 호출하도록 리팩터링 하면 됩니다."`
	- `(민섭님) ".. 알아서 잘 하실거라 생각합니다."`


**CCTV Analysis 코드 리뷰 당일**
- `(저) "레거시의 경우 하나의 프로세스 안에서 테스트 환경과 운영 환경을 같이 돌리는데, 환경을 분리해서 프로세스를 따로 돌리는게 맞다고 봅니다. 환경 분리를 하시지 않은 이유가 있으실까요?"` 
- `(팀장님) ";;그게 맞는데 .. 인프라 분리는 비용 때문에 어쩔 수 없다."`
	- 민섭님은 침묵 ..


**AisData 요청 당일**
- `(기획팀원) "인수인계 받은게 없어서 몰아요"`
- `(백엔드 팀장님) "저도 몰라요."`
- `(시현님) "저도 잘 모르는데; 프로젝트는 찾아 드릴게요"`


**AisData 코드 리뷰 당일**
- 코드 리뷰 및 기술 공유 
	- 1. 도커 컴포즈 기반 로컬 테스트 인프라 구성 
	- 2. MockHttp 패키지 기반 HttpClient 테스트 
	- 3. 단위 테스트 기반 일일 통계 리팩터링 (with. TDD)
- 발표 완료 후
	- `(민섭님) dynamic 키워드가 남아 있는데, 모델로 변환 가능한건 아닌지`
		- `(저) "아 .. 제가 누락했네요. 고치도록 하겠습니다."`
	- `(팀장님) "object와 dynamic 타입의 차이가 뭘까요?"`
		- `(저) "컴파일과 런타임에 타입이 결정된다는 것 말고는 잘 모르겠습니다"`
		- `(팀장님) "블라블라 (노트북으로 AI한테 질문한 내용을 그대로 읽으면서)"`
		- `(팀장님) "object와 dynamic에 대해 학습하셔서 공유해주세요"`
		- `(팀장님) "그리고 teams로 보내준 IRestFullApiService 리팩터링 해보세요"`
	- 💣 문제
		- `(팀장님) "3월 말에 e-log 프로젝트 할 거 같은데, 나랑 같이 합시다. 설계를 혼자서 어떻게 해야 할지 모르겠어요. 기술은 C#을 할지 Node를 할지 정해진게 아직 없어요. 원피스에 나미 알죠? 나미가 수기로 종이 쓰던거 그겁니다. 대기업에서도 일부 도입한 서비스로 아날로그를 디지털 전환할 필요있는 핵심 사업입니다."` 


> 그 동안 살펴봤을때 테스트, 설계 , 리팩터링에 대해 피드백을 받고 싶었으나 
> 전혀 상관없는 질문들을 통해 지적 우월감을 뽐내고 싶어 하는 것으로 보였음


백엔드팀 회의 
- `(팀장님, 약간 격양된) "이제 나도 어떻게 해야 할지 모르겠어요. 저는 할 만큼 방어했는데, 이렇습니다. (강성필 이사님과의 대화 내용을 공유하면서) 저도 모르겠어요. 얼마전에 대표님이 앵귤러를 리액트로 바꾸는데 얼마 걸렸답니다. 이제 구현만 하는 개발자는 필요없다. 설계자가 필요하지. 그러니깐 공부하세요!! 생산성이 중요해졌다는건데 생산성이 안나오는 사람은 QA로 좌천될 겁니다. 그리고 QA로도 안되면 떠나셔야 하구요. 앞으로 프로젝트 팀 단위로 움직일거니 문제 생기면 팀 단위로 해결하세요"`
	- 지난 3개월간 기술 부채를 혼자 해결하면서 지켜봐온 바로 이때 확신함. 
		- 자기 객관화가 없고, 근거없는 실력에 대한 자신감이 넘치는 것으로 보임 
		- 이로인해 본인의 역량을 뛰어넘는 일을 저지른 후 책임 전가/회피할 확률이 높다고 판단
		- <u>지금까지와 마찬가지로 e-log 프로젝트 시작하면 바쁘다는 핑계를 대고 빠질테고, 프로젝트 팀 단위로 움직이라고 했으니 PM과 알아서 하라고 할 확률이 너무 높다고 판단함</u>
	- 입사한지 6개월째 무료 IDE에 자비로 AI 사용중인데 무엇하나 지원 없이 기술 부채 프로젝트 3개 해결해야 하는데 이런 말을 들어야 하는지 어이가 없었음 
	- `AI-Washing`이라고 2025년 미국 전체 해고자 중에 AI로 해고된 건 5% 뿐이고, 나머지는 코로나때 너무 많이 뽑아 회사 이미지를 지키면서 재원 확보 위해 정리한거라고 분석 기사를 봄
	- 얼마전 긱 뉴스에서 아마존에서도 AI 사용하다가 장애가 2번 발생해 내부 정책을 바꿨다는 기사를 읽음 


**팀장님 자체 개발한 AzureFrameWork 모듈 코드 리뷰 당일** 
- `(팀장님) "이거 만드는데 AI랑 얼마나 걸렸을걸로 보이나요?"`
- `(나) "글쎄요. 한 한달 정도 걸렸으려나요?"`
- `(팀장님) "일.주.일. 걸렸어요"`
	- AI 끼고 무에서 유를 만드는 것은 쉽다. 하지만 테스트 프레임워크 하나 없이 웹 프로젝트로 주석 처리하면서 수동 테스트한 프레임워크의 기술 부채가 눈에 띄었다
- **질문 시간**
	- `(나) "cosmos 조회 속도가 빨라지셨다고 하셨는데, 모델 맵핑 때문인건가요"`
	- `(팀장님) "네."`
		- 단 건 조회 한건 기준으로 속도가 빨라졌다고 설명하는데 .. 인메모리 캐싱 쓰면 처음 조회가 느리지 두번째 조회가 빠른건 다 아는 사실이다.. 오히려 테스트를 더 많이 해봐야 하지 않아 싶었다.
	- `(나) "저 질문 또 있습니다. 메타 캐쉬를 내부적으로 사용하신다 했는데 캐시 초기화나 캐시가 다 차면 어떻게 되나요"`
	- `(팀장님) "서버 재시작 하시면 됩니다."`
		- 💩 가용성이 필요한 내부 서비스에 도입하기에 검증되지 않는 프레임워크라는 생각이 듦
		- 💩 AI가 CRUD 빠르게 만들어줄수는 있어도, 엔지니어링 관점에서 캐시 부분이 이상은 없는 부하 테스트와 모니터링을 통해 검증을 해야 하는데 .. 웹 프로젝트에 하드 코딩된 테스트를 작성하고 주석 처리하는 형태로 개발해 온 것으로 확인이 되었다.
		- 회귀 테스트는 전혀 안되고, 테스트는 어떻게 하는지 본인만 알아 볼 수 있는 모듈로 `기술 부채 전파`가 걱정이 되었다.

> 기술적 질문에 대한 답변을 할 수 없고, AI 기반한 CRUD 생산성을 강조한다는 점에서 .. **기술 리더십의 부재가 확인**되었다.

---

4월 

4월 첫주 
- 민섭님, NoonData 코드리뷰 
	- "리팩터링을 어떻게 하는지" 질문함 
		- 분할 정복 기법과 단위 테스트에 대해 구두로 설명 
	- "내가 TDD를 하게 되다니..." 라는 자조 섞인 말을 함

4월 둘째주
- AzureFrameWork 공통 모듈을 사용하면서 빌더를 외부에서 주입 받지 못하는 이슈가 발생함 
	- internal이라 기본 생성자 초기화가 불가능
	- 모듈 csproj에 접근 허용 설정을 하지만, 유지보수 관리가 어려워짐 (편법)
	- 레파지토리 테스트 하는데도 생성자에 기술이 노출되서 테스트가 오염됨
- 팀장님 : 빌더를 mock으로 대체 하면 안되나 
	- mock에 대한 개념과 테스트 대역에 대한 개념 전부 없다고 판단 .. 

4월 7일 백엔드팀 회식 
- xUnit 테스트 템플릿을 만들면 팀원이 학습, 자기계발을 할까 고민이라고 팀장님한테 말함 
- 자기는 "AI 때문에 개발자는 이제 끝났다"고 생각한다고 말함 (시현)

4월 10일 로거 데이터 재입력 이슈 발생 
- 서버 장애, 병목, 느린 속도 등 
- 로컬 실행시 timezone 이슈로 인해 데이터가 잘못 연산되어 들어간다는 것 생각남 
- 대표방 + 팀장방에 공유된 내용 보고 대표님이 나한테 자기가 만든 코드에 결함이 없다면서 결핍을 드러내는데 팩트로 두들기자니 .. 무지함과 기술 리더십 부재이면서 간섭을 할거라는 생각 밖에 안듦 

4월 10일 오후 2시 타운홀 미팅 

|**대표의 헛소리**|**엔지니어링 & 비즈니스 관점의 진실**|
|---|---|
|**"무슨 업무 하는지 모르겠다"**|지누님이 **팀즈로 상세히 보고**했음에도 이해를 못 하는 겁니다. 본인의 문해력과 기술 이해도 부족을 직원들의 보고 탓으로 돌리는 전형적인 **'남 탓'**이죠.|
|**"무슨 말 하는지 모르겠다"**|지누님이 설명한 **API, 로케일, 정합성 보정** 같은 전문 용어를 알아들을 실력이 안 되는 겁니다. 본인이 만든 로직의 결함을 지적당하니 자존심 상해서 내뱉는 **'방어 기제'**고요.|
|**"쓸데없는 프로젝트 한다"**|**GICOMS**는 SK와 연계된 외부 협업이자 선박 위성 데이터라는 **회사의 핵심 파이프라인**입니다. 이걸 '쓸데없다'고 치부하는 건 본인이 경영하는 회사의 비즈니스 모델조차 부정하는 꼴입니다.|

---

4월 24일 토끼방
gicoms 코드 리뷰 
1. 메일 발송 실패시 재시도 관련 
	1. 한 시간이나 대기타는건 너무 느리다 (`병건`)
	2. SendGrid 서비스가 다운되지 않는 이상 재시도 (지수 백오프 5회) 전략이 유효 할것으로 판단 (`나`)
		1. 다만 예외 발생시 다른 선박에 대한 비즈니스 로직이 종료되지 않도록 try-catch 통해 로그만 출력 하도록 함 (per ship 전략)
2. 폴리곤 좌표 관리를 데이터 베이스나 공통 모듈로 뽑아 관리하면 좋을거 같다 (`시현`)
	1. 프론트에서 폴리곤 사용하는 경우가 많은데, 공통 모듈을 하나 뽑아 기능을 관리하는게 좋을듯하다 
	2. 이런 닷넷 토폴리지 슈트 패키지가 있다는 걸 처음 알았는데 유용할거 같다. 
3. 데이터베이스에 비즈니스 로직을 옮겨서 거기서 각 위성 위경도 좌표가 폴리곤 영역에 들어가는지 검사하는 방식은 어떠하냐 (`팀장님`)
4. VL에 이메일 인증 테이블이 있는데, 거기다가 이번 신규 서비스의 테이블을 타입 구분해서 넣었으면 안되는가 (`가영`)
	1. EF Core에 엔티티 상속 기능이 있는지 모르겠다. 
		1. 아마 있을 거다
	2. 도메인 중심 설계로 구현을 했는데, 객체 라이프 사이클이 상이하고 타입 필드로 구분하게 될 경우 객체 생성자 초기화시에 null로 초기화하는 필드가 발생가능하다. 객체 생성자 초기화시 모든 필드가 초기화되지 않고 일부 필드가 초기화되는 경우 해당 객체는 응집도가 낮다고 표현할 수 있다. 그러한 객체 필드는 null로 초기화 되기 때문에 의도치 않은 버그를 유발하면 유지보수를 어렵게 할 수 있다 (`나`)
5. 읽기 전용 엔티티를 사용해서 디비에서 직접 조회하도록 레파지토리 로직을 구현, 테스트 완료 
	1. EF Core에서 fluent api 로 연관관계를 설정한 후 include를 사용하면 객체를 모두 가지고 와서 거기서 필터링이 가능하다. (`팀장`)
	2. 저에게 필요한건 3개 테이블에서 5개 필드이기 때문에 굳이 연관관계 설정할 필요 없이 join을 통해서 작업을 처리할 수 있었다. 읽기 전용인데다 영속성 컨텍스트 기능을 활용하지 않기 때문에 가볍게 구현 가능했다. (`나`)
		1. navigation property를 사용하여 객체 그래프를 탐색하거나 영속성 컨텍스트의 기능을 사용할 필요가 없는 프로젝트이다. 
		2. 더군다나 연관관계를 설정하게 되면 관리에 대한 책임이 해당 프로젝트에 오기 때문에 좋지 않다고 생각했다.

4월 27일 백엔드팀 주간 회의 오전 10시 40분
- 오전 9시 30분 경 `가영님`이 gicoms 서비스 관련하여 관리자 페이지에 토글 버튼이 늘어나는데에 UI/UX 상으로 좋지 않아 보인다고 질문함 
- Teams - 단톡방에서 이 부분에 대한 아이디어를 `가영님`이 공개적으로 함 
- 이때 팀장님이 `ㅋㅋㅋ` 하면서 인프라적인 부분에 대해 설명해주겠다고 끼어듦
- 주간 회의 끝나고 for-shore팀 인원만 남음 (병견, 시현, 가영, 용한, 상민, 나)
- `(팀장님) 우리는 EXTERNAL API 프로젝트가 있고 이를 통해 외부 고객사에 데이터를 공급하고 있다. 이런게 있는걸 안다면 개발자가 PM한테 얘기해서 작업을 진행하도록 했어야 했는데, 그런것도 모르고 금요일에 코드 리뷰나 하고 있고 ㅉㅉ`
	- 나 : 아니요. 저는 프로젝트 분리 하는게 맞다고 생각합니다. 
	- 팀장 : 선박이 750척이 있는데..
		- 나 : 위성 서비스를 이용하는 선박은 350척 정도인데요
		- 팀장: 하?! 그럼 전선박 데이터를 다보낼건가 ? (혼자 흥분하면서) 
		- 나 : 아뇨 테스트 기간에 고객사에 선박 몇 척만 정해서 데이터 전송/파싱 잘되는지 확인만 하면 됩니다. 전체 선박을 보낼 필요가 전혀 엇습니다.

> 4월 4주간 주간 회의 통해 신규 프로젝트 진행한다고 보고를 했었는데 첫주에 아무 얘기 안하고, 마지막 주에 와서 공개적으로 팀원들 앞에서 기획자와 함께 비난하는 이유를 이해할 수 없었음


4월 30일 오후 7시 팀 회식 (부전동 해신)
- 팀장 : 진우님이 잘하니깐 데이터인데 다 가져 가면 되겠네?
- 주변 팀원 : 그러게 말입니다.

> 치켜 세우기 전략을 통해서 고립 시키려는 모습이 확인됨
> 말없이 소주 한 병 마시고, 연장자분한테 아이를 키우는거에 대한 행복에 대해 질문함


