
# 📌 오늘 배운 것 (Today I Learned) 
## 날짜 
`2025년 12월 03일`

## 카테고리
`#CSharp`, `#XUnit`, `#init`

## 주제: 
### 1. 문제 상황 또는 학습 배경
- 레거시 function app의 코드 레벨 분석을 시작
- 외부 인프라 호출하는 헬퍼 클래스와 강결합이 되어 있고, 접속 정보도 하드코딩으로 기재되어 있는 상태
- 테스트 하기 힘든 구조이다보니 xUnit 템플릿 프로젝트 생성하여 리팩터링을 하기 위한 기반 마련
### 2. 핵심 내용 / 개념 정리

>[!note] 닷넷 진영에서는 xUnit 템플릿 프로젝트를 따로 만들어서 테스트 코드와 패키지를 관리한다. 이를 통해 배포 프로젝트에서 테스트와 관련 내용을 제외할 수 있다함

> [!info] dotnet cli 사용하기 위해 PowerShell 사용

`xunit 템플릿 프로젝트 생성 (루트 디렉터리)`
```shell
$ dotnet new xunit -n LoggerDataV8.Tests
```

최상위 루트 sln 경로에서 실행
```shell
$ dotnet sln add .\{프로젝트명}.Test\{프로젝트명}.Tests.csproj
```


`테스트 프로젝트에 부모 프로젝트 참조 추가`
```shell
$ dotnet add {프로젝트명}.Tests/{프로젝트명}.Tests.csproj reference {프로젝트명}/LoggerDataV8.csproj
```

프로젝트 계층 구조를 나타내기 위해 최상위 루트 sln을 vim으로 열어 수정한다
```text
1. {프로젝트명}.Tests가 추가되었는지 확인
Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "{프로젝트명}.Tests", "{프로젝트명}.Tests\{프로젝트명}.Tests.csproj", "{53087D2C-F8BF-44BF-8D5D-9B1A564CD0F9}"
EndProject

2. GlobalSection(NestedProjects) = preSolution 하위에 설정 추가 
	//..
	//{프로젝트명}.Tests 이고 옆에가 Functions 폴더 GUID
	{53087D2C-F8BF-44BF-8D5D-9B1A564CD0F9} = {78DCD72B-5B0A-4249-A990-366F77F74941}
   EndGlobalSection
```
- 프로젝트 재시작하게 되면 Functions 디렉터리 안에 신규 xUnit 프로젝트가 포함된 구조로 탐색기 표출됨


그외. csproj에 수동으로 패키지 변경 후 
```shell
$ dotnet restore
```
