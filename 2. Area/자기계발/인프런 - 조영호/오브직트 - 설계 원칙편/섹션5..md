
### 5-1. 단일 책임 원칙 (클래스 분리하는 방법 중 하나)
> Schedule 예제 클래스 생성

단일 책임 원칙에서의 책임은 클래스가 변경될 이유를 말한다
반면, 책임 주도 설계의 책임은 객체가 다른 객체와 협력하기 위해 제공해야 하는 서비스의 개념

단일 책임 원칙 > 책임 주도 설계 개념보다 큰 개념 


**응집도(Cohesion)**
- 모듈 내부 요소들이 **함께 변경**되는 정도
- **높은 응집도** - 모듈 전체가 동일한 이유로 변경
- **낮은 응집도** - 모듈 간 부분이 서로 다른 이유로 변경

> 단일 책임 원칙은 응집도가 높은 클래스를 만드는 원칙이다

> 변화율 (Rate of Change) : 변경의 이유에 따라 클래스를 분리하라

> 하나의 클래스에 변경할 이유가 여러개라면 변경시점이 달라 사이드 이펙트 발생가능하다.
> 고로 이를 해결하기 위해 **변경의 이유에 따라 클래스를 분리**하자
- 코드를 수정하는데 필요한 읽을 양이 줄어든다


**결합도(Coupling)**
- **외부**의 다른 모듈에 의해 **함께 변경되는 정도**
- **높은 결합도** - 외부의 모듈이 변경될 때 함께 변경되는 빈도가 상대적으로 높음 
- **낮은 결합도** - 외부의 모듈이 변경될 때 함게 변경되는 빈도가 상대적으로 적음


**🔎 단일 책임 원칙 관점에서 Schedule 클래스 분석**
- `includes(..)`
	- 일정 확이 방식이 변경될 때 수정된다 
- `toJson()`
	- 출력 포맷이 변경될 때 수정된다
- 서로 다른 이유로 수정되는 메서드가 모여 있어 응집도가 낮다
- json format을 변경하는 라이브러리를 적용하려면 다른 메서드에 영향 줄 수 있다
- 결국 단일 책임 원칙 위반하고 있다. 
	- 응집도가 낮고 결합도가 높은 Schedule 클래스
- 해결방법 
	- 분리합시다.
	- `변경의 이유가 다른 메서드를 클래스로 분리하자`

> [!info] 단일 책임 원칙의 핵심 
>- **클래스는 작아야 한다**
>- 단 하나의 변경 이유만 가질 정도만 클래스는 작아야 한다
- 나쁜 소식
	- **문제는 SRP는 이해하기도, 적용하기도 어렵다** 
	- 모든 강의 중 SRP가 제일 어렵다
	- 동일한 기능, 다양한 설계 
		- 기능이 동일하더라도 변하는 부분이 달라지면 책임도 달라지기 때문
- 좋은 소식 
	- **단일 책임 여부를 결정하는데 도움이 되는 가이드가 존재**

---
### 5-2. 단일 책임 원칙 가이드
> Schedule 클래스 분리 + 데일리 스크럼 요구사항 추가


- 클래스를 분리 할때 적용하는 원칙이고 설명
- 도서. working effectively with legacy code


>[!note] 가이드1. 책임을 한 문장으로 적어본다
>클래스의 책임을 설명하는데 '그리고'나 '또는'이 들어가면 여러 개의 책임을 담당하는 클래스다

>[!note] 가이드2. 메서드를 분류한다
>이름이나 목적이 비슷한 메서드들을 그룹으로 묶어 나열한다. 이 메서드 그룹별로 클래스들을 나눌 수 있는지 살펴본다.
>

>[!note] 가이드3. 인스턴스 변수와 메서드 사이의 관계를 살펴본다
>일부 인스턴스 변수가 일부 메서드에 의해서만 사용되는지 살펴본다

>[!note]  가이드4. 서로 배타적으로 초기화되는 인스턴스 변수가 있는지 살펴본다
>어떤 변수들이 초기화될 때 함께 초기화되지 않는 인스턴스 변수들이 있는지 살펴본다

>[!note] 가이드5. 테스트하고 싶은 private 메서드가 있는지 살펴본다
>너무 많은 private (또는 protected) 메서드가 왔을때 테스트하고 싶은 private 메서드가 존재하는지 살펴본다.

>[!note] 가이드6. 외부 의존성을 찾는다
>데이터베이스 연결이나 외부 시스템과의 연동 등과 같이 외부에 위치하는 불안정한 의존성을 찾는다.



`Schedule 클래스`를 분석해본다
- 가이드1 적용
	- Schedule은 반복되는 일정을 확인**하고** 일정 정보를 JSON으로 변환한다
- 가이드6 적용
	- 외부의 Jackson 라이브러리에 의존하는 코드 분리 
	- Schedule이 영향 받을 수 있으니 별도의 클래스로 분리하는게 좋다
	- `ScheduleJson -> Schedule`을 의존한다

> 단일 책임 원칙의 가장 중요한 기준 
> - **요구사항 변경** : 요구사항이 변경되면 책임의 범위도 요구사항에 따라 변경

> [!info] 👷 데일리 스크럼이라는 요구사항 변경이 발생한다 (기존 클래스는 월간회의만 다룸)

가이드 4 적용
- 서로 다른 인스턴스 변수가 초기화이 다른 경우
가이드 3 적용

**함께 초기화되는 인스턴스 변수와 인스턴스 변수를 사용하는 메서드**
- 인스턴스 변수와 메서드 사이의 관계를 **기능 스케치**🎨로 나타낸다

**함께 사용되는 필드와 메서드를 그룹핑 -> 별도의 클래스로 분리**
- MonthlyPlan
	- 변수 : ordinal, dayOfWeek
	- 메서드 : include(day)
- WeeklyPlan
	- 변수 : dayOfWeeks
	- 메서드 : include(day)
- 인터페이스 생성 : RecurringPlan

**다형성을 활용하기 위해 인터페이스 추가**

>[!tip] (가이드3 적용 전에) 단일 책임 원칙을 적용하기 전에
>조합 메서드로 리팩터링 (ex. Game run() )


> [!tip] 핵심은 요구사항이 어떻게 변하는지 관찰하라
- 다음 시간에 가이드 5,6을 Game 클래스와 함께 다룰 예정 

---
### 5-3. 클래스 나누기 (✅ 다시 보기, 중요 - WorldMap 클래스)
> Game 클래스 예제 사용

> 말로서 Game 객체가 하는 일을 표현해본다 (단일 책임원칙을 지키고 있는지)

**단일 책임 원칙 사전 작업**
- 단일 추상화 수준 원칙(SLAP)을 만족시키도록 추상화 수준이 동일한 조합 메서드(composed method) 형태로 리팩터링

>[!info] 켄트 벡, 스몰토크 모범 사례 패턴
>프로그램을 하나의 명확한 작업을 수행하는 메서드들로 나누세요. 메서드 내의 모든 작업을 **동일한 추상화 수준**으로 유지하세요. 이렇게 하면 여러 개의 작은 메서드로 이루어진 프로그램이 만들어지고, 각 메서드는 몇 줄 정도로 짧아지게 됩니다.
- (260328) 조합 메서드가 생각나고, 서로 다른 이유로 변경이 된다면 단일 책임 원칙 가이드에 따라 클래스를 분리해야 한다는 게 아닌가 싶음

**가이드1에 따라 Game 클래스의 책임을 문장으로 설명해본다**
	게임이 종료될 때까지 루프를 실행**하고**,
	사용자에게서 입력을 받**고**,
	입력을 파싱**하고**,
	명령을 처리**하고**,
	처리 결과를 출력**한다**.

**가이드 2에 따라서 메서드 그룹을 나눈다(초안)** 
- 카테고리 6개로 분류
	- 흐름제어 
	- 플레이어 이동 
	- 지도 구조 관리 
	- 사용자 입력
	- 명령어 파싱
	- 화면 출력

>[!tip] 책임을 정리한 문장과 메서드 분류는 작업 방향을 정하기 위한 스케치
>클래스를 분리할 수 있을 정도로 정확할 필요가 없음 

**가이드3에 따라서 기능 스케치를 그려본다**  // 그림 참고
- 인스턴스 변수와 메서드 사이의 관계를 파악해본다 

```text
- 1. 인스턴스 변수를 먼저 추가한다
- 2. 인스턴스 변수를 사용하는 메서드 추가
- 3. 메서드 호출 경로 추가 
- 4. 함께 사용되는 인스턴스 변수들을 그룹으로 묶기
	- running 참조 그룹 
	- position 참조 그룹 
	- size, room 참조 그룹 
```

> [!tip] 메서드 분류는 추출할 클래스의 목적과 명칭에 대한 힌트를 제공한다

>[!note] 객체 지향 설계의 목표는 
>스스로 책임지는 자율적인 객체들의 협력을 이용해서 기능을 구현하는 것

> Game에서 WorldMap 클래스를 분리한다 (**코드 레벨 설명**)
- 지도 구조 관리라는 하나의 책임을 맡은 WorldMap 클래스는 SRP를 만족한다
	- 가이드5도 적용됨

---
### 5-4. 테스트 관점에서 분리하기 (✅ 다시보기, 중요 - CommandParser)
> Game 예제 사용

> 테스트하기 힘든 private 메서드가 있다면 책임이 많다는 신호이기도 하다. 책임을 옮겨서 public 변경 후 테스트 하도록 한다. (책임 분리 관점)


Game안에 private 있던게 WorldMap으로 옮기면서 public이 됨 
- 효과1. 테스트가 가능해짐 
- 효과2. Game 클래스 책임도 작아짐 

**CommandParser 클래스 생성, 책임 분리**

👷 Game과 CommandParser의 양방향 참조가 만들어짐 
- 문제1. 서로 의존하고 있으니 서로 변경 전파 가능
- 문제2. 객체 참조 동기화 로직이 필요함
	- 동기화 로직에 오류가 있으면 두 객체가 정상 동작 ❌
- 문제3. 테스트하기 어려워짐 (두 클래스가 모두 필요해짐)

> 메서드 차원에서 다시 고민해 봅니다. 

테스트와 관련 있는 **입력 파싱 로직** (parseCommand)
테스트와 관련 없는 **메서드 실행 로직** (executeCommand)

남은 문제는 이 두 메서드를 어떻게 연결할 것인가? (양방향 참조는 안된다💩)
- 중간 객체를 Command
- sealed interface로 선언 

> CommandParserTest 예제 코드 보여줌

> [!note] 어떤 클래스를 테스트 하기 위해
> 너무 많은 텍스트 픽스처를 생성해야 하거나  (높은 결합도🚨)
> 너무 많은 실행 경로를 테스트해야 한다면  (낮은 응집도🚨)
> 단일 책임 원칙을 위반한 것이다 
> 
> **✅ 테스트 범위에 따라 클래스를 분리하라**

---
### 5-5. 의존성 기준으로 분리하기 (✅ 캡슐화 원칙, tryMove 이동)
> tryMove 메서드를 새로운 클래스(Player)로 이동 시킨다. 
- 가이드5, 가이드3을 살펴본다

> Player 클래스 생성

> [!tip] 설계 품질의 척도는 단위 테스트를 통해서도 알 수 있다. (신호를 보낸다🚨)

> [!info] 테스트를 쉽게 작성할 수 있으면 클래스의 품질이 높을 수 있다

Player에 **콘솔 출력에 대한 의존성이 숨겨져 있어서** 테스트 하기 힘들어짐 
- Player 메서드에 대한 분석 
	- 서로 다른 의존을 가지고 있어 변경에 취약하다.
		- 1. 플레이어를 움직이는 로직이 변경될때 수정 : `tryMove()`
		- 2. 출력 메커니즘 변경될 때 수정 : `showBlocked()`, `showRoom()`
	- 변경관점에서 SRP를 위반하기 쉽다
- **가이드 6**을 봄

>[!note] 외부 의존성 관리를 위한 조언 
>외부 의존성과 관련된 책임을 하나의 클래스 내부로 캡슐화하라


Game이 Play 내부 구조에 의존하는 모양이 만들어짐 (결합도가 높아짐)
- 그래서 Game이 Play만 의존하도록 캡슐화를 한다는데 
- 이를 통해 Player 이외 다른 클래스에 대한 의존성을 제거할 수 있다함 

> Game 클래스로 showBlocked, showRoom 메서드를 옮긴다
> 그리고 Player 인스턴스 변수에 접근 가능하도록 getter를 제공
- Game, Play 간의 양방향 참조가 생성된다.
	- 양방향 참조를 추가하지 않고 콘솔에 출력할 방법은??
	- tryMove()를 살펴본다 
- 🔎 양방향 참조를 제거하기 위해 Game으로 tryMove 메서드 이동

> 🔎 리팩터링 후 Game 클래스는 Player의 내부 구조에 의존하고 있다. 변경 관점에서 Player 변경에 영향 받기 쉽다. -> 👷 Player를 경계로 내부 구조 캡슐화.

>[!note] 캡슐화 위반의 원인 
>- 데이터를 이용해서 좌표를 이동시키는 프로세스 (Game)
>- 프로세스가 사용하는 데이터 (Player)
>이는 곧 잘못된 책임 할당을 나타낸다(캡슐화가 깨짐)


> ✅ 묻지말고 시켜라, 디미터의 법칙 둘다 캡슐화와 관련있다.
- 두 법칙을 사용해 Player 객체를 리팩터링 해본다 
- 섹션 6으로

---

**Q. SRP 가이드라인 및 테스트 관점에서 볼 때, 어떤 경우에 특정 메소드를 다른 클래스로 분리하는 것을 고려해볼 수 있을까요?**

A. private 메소드가 테스트하기 어렵다는 것은 그 메소드가 클래스의 핵심 책임 외의 다른 책임을 숨기고 있을 수 있다는 신호예요. 분리하면 테스트와 책임 명확화에 도움이 된답니다.


**Q. 단일 책임 원칙(SRP) 적용 시, 콘솔 I/O나 외부 라이브러리 사용 등 '불안정한 외부 의존성'을 특정 클래스로 분리하는 주된 목적은 무엇일까요?**

A. 외부 의존성은 변경될 가능성이 높아 불안정해요. 이를 특정 클래스에 격리하면, 외부 대상 변경 시 해당 클래스만 수정하고 다른 클래스는 영향받지 않아 유지보수가 쉬워집니다


**Q. 두 클래스 A와 B가 서로를 직접 참조하는 양방향 의존성(Bidirectional Dependency)이 발생했을 때, 객체 지향 설계 관점에서 주로 어떤 문제가 발생할까요?**

A. 양방향 의존성은 두 클래스를 하나의 큰 덩어리처럼 묶어요. 한쪽을 바꾸면 다른 쪽도 바꿔야 할 때가 많고, 테스트할 때도 두 클래스 모두 필요해 복잡해집니다.


